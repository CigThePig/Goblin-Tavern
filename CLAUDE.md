# Goblin Tavern

A text-based goblin tavern management simulation built **simulation-first**: a headless rules engine ships before any card content, UI, or narrative writing is added on top.

## Current Status

**Phases 1–40 (initial build + expansion arc): done.** The headless simulation lives under `src/sim/` (core engine, registries, ~26 domain modules under `src/sim/modules/`, a content layer under `src/sim/content/` covering naming, cultures, factions, suppliers, npc, staff, tavern, events, and text, state with Zod schemas, and testing utilities). Phase-by-phase test coverage runs from `tests/sim/phase2.structure.test.ts` through the repair-pass tests (currently up to `phase97.missedOpportunityRoundtrip.test.ts`). Run `npm test` (Vitest) and `npm run typecheck` (TypeScript) to validate changes.

**Phases 41–97 + 117–120 (post-Phase-40 repair pass + UI/UX clarity + web test stack): done.** The repair pass landed Tier 0 infrastructure (ISSUE-001…004), Tier 1 roster grows (ISSUE-010…012; ISSUE-005…009 superseded by the Tier 1.5 arc), the **Tier 1.5 Rare Ingredients Economy** arc (ISSUE-025…033, phases 65–73), Tier 2 family rewrites (ISSUE-013…019), Tier 3 polish + audit fixes (ISSUE-020…057), the Tier 5 UI/UX clarity passes (ISSUE-078/079, phases 117–118), and the combined web test-coverage + `$derived` audit (ISSUE-058/059, phases 119–120). The web "More" tab + save slots + first-encounter hints + difficulty work (phase 98) shipped as part of the web chassis and is tracked retroactively as ISSUE-080.

**Phase 121 (Living Cast arc, Phase A): done.** Bounded selection vocabulary attached to staff and regulars (`specialty`, `blindspot`, `affinity[]`, `voiceProfile` with 4 axes + optional verbal-tic). Lives under `src/sim/content/cast/`; reuses the `staff_identity` / `regular_identity` named RNG streams; stored as plain JSON on `StaffState.castAttributes` and `RegularWorldState.castAttributes`. Tracked as ISSUE-090.

**Phase 123 (Living Cast arc, Phase C): done.** Compositional card runtime — typed `Snippet` / `SnippetPool` / `SnippetCondition` (eleven framework primitives + Phase-B's `voiceAxis` atLeast/atMost + `verbalTic` reads of `CastAttributes`), deterministic `assembleSlots` / `pickSnippet` with shared FNV tie-break, and the `defineCompositionalCard` factory. First live compositional template `drinkOrderCard` rides `regular_customer / relationship_test / during_service` seeds with Phase B's 17-snippet `order_line` pool + 5-snippet `manner_note` pool. The eight hand-written templates ship unchanged; `REQUIRED_CARDS` holds the mix. Tracked as ISSUE-092.

**Phase 124 (Living Cast arc, Phase D): done.** Six structural gates (framework §6) implemented as a reusable library at `src/cards/compose/gates/` — coverage, specificity-gradient, voice-bounds, sim-coherence, determinism, diversity — plus a composite `runAllGates` runner Phase E imports unchanged. Two additive optional fields on `SlotSpec` (`wordBudget?`, `claimMode?`) move Phase B's locked numbers from the doc into the slot data; `drinkOrderTemplate` now carries `order_line ≤ 12 words`, `manner_note ≤ 10 words`, both `claimMode: 'flavor'`. Diversity sampler uses `createRegularCastAttributes` with a deterministic `prando`-seeded RNG to reproduce the real `[-1,0,0,1]` cast distribution. 28 new gate tests cover happy paths against the real drinkOrder pools and intentionally-bad fixtures proving each gate fails when it should. Tracked as ISSUE-093.

**Phase 125 (Living Cast arc, Phase E): done; retired in Phase 130.** Build-time generation pipeline at `scripts/generate-pool/`: a Phase-B-format spec under `specs/cards/` flows through load → prompt → Claude (Sonnet 4.6 by default; `cache_control` on the spec/exemplars) → parse → Phase-D `runAllGates` → retry (budget 3, violations + dedupe rejections fed back to the model) → dedupe (Levenshtein-on-canonical at 0.85 within slot, canonical equality across slots) → deterministic emit. Output lands directly at `src/cards/compose/pools/<templateId>/<slotId>.ts` so PRs show the diff against Phase-C's hand-authored pool; emitter sorts by `(specificity, id)` so identical model output is byte-identical on disk. The first spec, `specs/cards/drink_order.spec.yaml`, is the YAML from `living-cast-arc-phase-b.md` lifted into a real artifact. `.github/workflows/generate-pool.yml` runs the pipeline on `workflow_dispatch`, reads `ANTHROPIC_API_KEY` from repo secrets, and opens a PR via `peter-evans/create-pull-request@v6`. 43 new tests cover spec load, prompt build, model-output parse, dedupe (with a per-pair regression net against the committed `order_line` pool), gate adapter, retry loop with mocked model, deterministic emit, and end-to-end convergence on the real spec — no network is touched in tests. Tracked as ISSUE-094. **Retired in Phase 130 / ISSUE-099 (Voiced Surface arc, Phase 4): authoring is now an in-repo Claude Code plan-mode run; the 0.85-dedupe guarantee moved into the gate library as a seventh `checkDedupe` gate.**

**Phase 126 (Living Cast arc, Phase F, first situation): done.** Second compositional template — `staffAsideCard` attached to `staff_identity / relationship_test / morning_prep`, voice register `staff_quarters` (back-of-house, pre-shift, owner-facing). Hand-authored pool (user opted out of API spend) following the Phase-B convergence pattern; matching spec lives at `specs/cards/staff_aside.spec.yaml` as design record + future-regeneration input. Phase-E hardcoded the first template's slot ids under `HardBoundsSchema.order_line/manner_note/sim_backed_hook` and `PositiveExemplarSchema.order_line/manner_note` — generalised in the same phase to `perSlotWords: Record<slotId, number>` and `slotLines: Record<slotId, string>`; `drink_order.spec.yaml` migrated to the new shape (same content). Gate harness extended with `buildStaffDeterminismSamples` / `buildStaffDiversitySampler` so all six gates plus the composite `runAllGates` runner exercise `staffAsideTemplate` against the real `[-1,0,0,1]`-perturbed staff distribution. Full suite green at 1856/1856 across 155 files. Tracked as ISSUE-095.

**Phase 130 (Voiced Surface arc, Phase 4): done.** Retired the Phase-125 build-time generation pipeline (`scripts/generate-pool/`, `.github/workflows/generate-pool.yml`, the strict `GenerationSpecSchema` loader, and the `@anthropic-ai/sdk` / `yaml` / `tsx` dependencies). Authoring snippet pools is now an in-repo Claude Code plan-mode run — read the spec under `specs/cards/`, author pools directly in `src/cards/compose/pools/<template>/<slot>.ts`, run `runAllGates` + per-situation tests, iterate to green, commit. The dedupe structural guarantee (canonicalised-text Levenshtein similarity ≥ 0.85 within a slot, canonical equality across slots) moved from the pipeline into `src/cards/compose/gates/dedupe.ts` as the seventh gate `checkDedupe`, wired into `runAllGates` alongside the six framework gates and exercised against both committed templates (`drinkOrderTemplate`, `staffAsideTemplate`). The deterministic-emit guarantee retires cleanly with its emitter (hand-authored pools live in readability order; no automated re-run to be byte-stable across). Standing authoring-loop recipe in `docs/plans/voiced-surface-arc.md` Appendix A. Resolves a Codex P1 on `specs/cards/supplier_reliability.spec.yaml` (the new spec's `appliesTo` / `simSignalsInUse` / `loopback` keys were rejected by the strict schema; retirement makes them legitimate without schema churn). 10 new dedupe gate tests; 43 retired pipeline tests removed. Tracked as ISSUE-099.

**Phase 131 (Voiced Surface arc, Phase 5): done.** Title becomes a composed `SlotSpec` on the two compositional templates (`drinkOrderCard`, `staffAsideCard`). New per-template title pools at `src/cards/compose/pools/<template>/title.ts` each ship 5 snippets — one unconditional fallback plus four voice-axis-conditioned variants (terse, warm, formal, florid) — all ≤ 6 words. Template glue (`buildDrinkOrderTitle`, `buildStaffAsideTitle`) reads `filled['title']` and prepends the actor display as `${display}: ${snippet}` with no clamping, so a long display name extends the title rather than truncating with `"…"`. `formatTitle` import dropped from both compositional templates; `composeTitle` / `formatTitle` themselves stay alive for the nine still-legacy templates until Movement II migrates each. The voice-bounds gate gains two new failure reasons (`trailing_ellipsis`, `duplicate_token`) scoped to slots whose advisory role is `'title'` — body-slot snippets like the `trails_off` verbal-tic line on `aside_line` keep their authored ellipsis. New `VOICE_BOUNDS_REASONS` frozen tuple exported through `src/cards/compose/gates/index.ts` so tests match by name. `runAllGates` integration test extended to include the new `title` slot in per-template diversity config (sampleSize 100, minDistinct 3 against the same `[-1,0,0,1]`-perturbed actor populations). Per-template integration tests assert no `"…"` / `"..."` in the rendered title and per-seed determinism. 5 new voice-bounds gate cases; full suite 1942/1942 across 160 files. Tracked as ISSUE-100.

**Phase 132 (Voiced Surface arc, Phase 6): done.** Choice & consequence voice. `drinkOrderCard` and `staffAsideCard` now compose their `CardChoice.label` and per-effect `CardChoice.previewEffects` through the snippet pipeline; the sim remains the mechanical source of truth (verb / targetId / shape and per-effect kind / target / amount / tags unchanged). New helper `composeChoicesFromSeed(seed, state, { labelPool, previewPool, maxPreview })` in `src/cards/cardHelpers.ts` builds synthetic `optional: true` `SlotSpec`s per response slot (`choice_label::${slot.id}`) and per immediate effect (`effect_preview::${slot.id}::${idx}`), threading the iteration as a new `ConditionContext { currentResponseSlot?, currentEffect? }` into `pickSnippet`. Pool misses pass `slot.labelHint` / `effect.readable` through verbatim — never an invented string. `buildChoice` gains two strictly-optional `ChoiceOverrides` fields (`label`, `previewEffects`); the six legacy templates' `buildChoicesFromSeed` call sites are unchanged. Four new condition primitives land in `src/cards/compose/types.ts` and `src/cards/compose/conditions.ts`: `responseVerb { anyOf }`, `responseShape { anyOf }`, `effectKind { anyOf }`, `effectTag { tag }` — all flat data, no closures, no OR/NOT/nesting; each returns `false` when its required `ctx` field is absent so body / title slot evaluation (no context passed) is unaffected. `evalCondition`, `pickSnippet`, and `assembleSlots` thread an optional `ctx: ConditionContext = {}`. New pools at `src/cards/compose/pools/{drinkOrder,staffAside}/{choiceLabel,effectPreview}.ts` each ship 6 snippets covering verb-gated (appease / ignore) × voice-axis (terseness / warmth / formality / floridity) combinations. The diversity gate gains an optional `pickContext?: (sample, i) => ConditionContext` so it can exercise the new primitives in pool-level testing (existing call sites omit the field and behave unchanged). Sim-coherence for previews holds structurally: each composed line corresponds 1-to-1 to a real `EffectPreview` because the helper iterates `profile.immediateEffects` per choice — no new gate detector needed. Specs (`specs/cards/drink_order.spec.yaml`, `specs/cards/staff_aside.spec.yaml`) gain matching slot entries as design records. 19 new tests (13 condition unit cases + 6 mechanical-equivalence integration cases) plus 2 new `runAllGates` cases driving both templates' pools through every applicable gate. Tracked as ISSUE-101.

**Phase 133 (Voiced Surface arc, Phase 7, first Movement II migration): done.** Staff & Personnel cluster. `staffAsideCard` extended with a sim-backed `establishing_line` slot (11 snippets — fallback + middle rung gated on `signalEquals staff.stress|staff.fatigue`, `pressureRising staff_burnout|staff_loyalty_risk`, `memoryPresent identity|warning`, `repeatCount staff atLeast:3` + 2 two-condition top-rung snippets). The `buildStaffAsideBody` grounding fragment that previously appended `seed.textIngredients.sensoryDetails[0]` as the body's last line is gone — body shape is now `[establishing_line, aside_line, manner_note?]`. New `staffBurnoutCard` (template id `staff_burnout.staff_request`, file `src/cards/templates/staffBurnout.ts`) replaces the legacy hand-written `staffRequestCard` for `staff_burnout / staff_request / morning_prep` — six slot pools at `src/cards/compose/pools/staffBurnout/` (title, establishing_line sim-backed, reaction_line, manner_note, choice_label, effect_preview) mirroring the staffAside shape; voice register `staff_quarters` shared with staffAside. Legacy `staffRequest.ts` deleted along with its imports in `src/cards/{templates,}/index.ts` and its blocks in `tests/cards/templates.test.ts` and `templates.voice.test.ts`. The legacy template's `timings: ['closing']` declaration never matched the real `issueSeedGenerators.ts:1153` output (`morning_prep`) — staff_burnout seeds were falling through to the fallback in production; the new template is the first card to actually handle the family. Specs at `specs/cards/staff_burnout.spec.yaml` (new) and `specs/cards/staff_aside.spec.yaml` (extended). 15 new staffBurnout integration tests, 1 new staffAside regression test ("never raw fragment"), 1 new `runAllGates` block (staffBurnoutTemplate clears all seven gates with the staff cast perturbation samplers). Full suite 1980/1980 across 163 files. Also folded in three PR #108 regression fixes surfaced by the first clean-baseline run: SessionStart hook in `.claude/settings.json` running `npm install --no-audit --no-fund` so fresh web sessions land bootstrapped; `tests/cards/compose/phase132.responseConditions.test.ts` typecheck error from `IssueSeedFamilyId | string` widening; `web/src/lib/cards/CardRenderer.svelte` Svelte 5 keyed-each duplicate-key crash on composed previews. Tracked as ISSUE-102.

**Phase 134 (Voiced Surface arc, Phase 8, second Movement II migration): done.** Regulars & Complaints cluster. The legacy `customerComplaintCard` (which hand-handled both the `regular_customer / complaint` and `customer_complaint / complaint` families through one fragment-dump template) splits into two compositional templates partitioned by family: `regularComplaintCard` (new, `src/cards/templates/regularComplaint.ts`, id `regular_customer.complaint`) covers the loud branch of the `regular_customer` family — partitioning that family by type alongside `drinkOrderCard` (mild `relationship_test` branch, Phase 123); `customerComplaintCard` (rewritten in place at `src/cards/templates/customerComplaint.ts`) covers the cohort `customer_complaint` family and finally centres the card on its own subject by reading `state.customerGroups[id].label` instead of the legacy template's regular-only resolver. Both templates carry the same four-slot body — title + sim-backed establishing_line + flavor reaction_line + optional flavor manner_note — plus Phase-6 composed choices and effect previews. Voice register `tavern_floor` (shared with drinkOrder) for both. Twelve new pool files at `src/cards/compose/pools/{regularComplaint,customerComplaint}/` and two new specs at `specs/cards/{regular_complaint,customer_complaint}.spec.yaml`. **Movement I loopback:** four new band signals added as an additive extension of Phase 127's signal surface — `regular.irritation`, `regular.loyalty`, `customer_group.satisfaction`, `customer_group.loyalty` — wired through `src/sim/signals/{types,bands,numeric,query,index}.ts` mirroring the existing staff/faction/supplier/area bands. Without them the templates would have anchored only on `pressureRising` and `memoryPresent`; with them the establishing line states the very meter that triggered the seed ("Their patience snapped a quarter-hour back", "Their satisfaction's been bottoming out all evening"). 33 new template tests (17 regularComplaint + 16 customerComplaint), 4 new `runAllGates` blocks (two template-level + two ad-hoc choice-pool), 13 new signal-surface tests for the four new bands. The `tests/cards/templates.test.ts` legacy block splits into Template 2a / 2b (one per new template); `tests/cards/templates.voice.test.ts` swaps the legacy `customerComplaintCard voice` block for two new ones; `tests/cards/templates.drinkOrder.test.ts` "does not steal the complaint variant" assertion now points at `regularComplaintCard`. Tracked as ISSUE-103.

**Phase 135 (Voiced Surface arc, Phase 9, third Movement II migration): done.** Suppliers, Stock & Debt cluster. Three new compositional templates land in `REQUIRED_CARDS`: `supplierReliabilityCard` (new, `src/cards/templates/supplierReliability.ts`, id `supplier_relationship.supplier_offer`, voice register `trade_floor`) replaces the legacy hand-written `supplierOfferCard` and emits the Phase-3 / ISSUE-098 converged spec at `specs/cards/supplier_reliability.spec.yaml` verbatim as TS pools — the broken-screenshot template that started the arc is gone. `stockShortageCard` (new, `src/cards/templates/stockShortage.ts`, id `stock_shortage.warning`, voice register `back_of_house`) and `debtRentCard` (new, `src/cards/templates/debtRent.ts`, id `debt_rent.debt_pressure`, voice register `office_quarters`, timing `end_month`) are the first dedicated cards for those families — pre-Phase 9 every `stock_shortage` and `debt_rent` seed routed to `fallbackCard`. Both ownerless templates are **narrator-voiced**: the seeds have no `primaryActor` (stock subject is a stock item; landlord is a `systemRef` per audit pass 1 §5.3), so every snippet pool is free of `voiceAxis` / `verbalTic` conditions — variety comes from state perturbation (rising pressures, prior-choice memories, calendar tone tags, severity bands). Cohort routing for the miners cohort and a player-owner voice profile are deferred as design-intent. Eighteen new pool files at `src/cards/compose/pools/{supplierReliability,stockShortage,debtRent}/`, two new specs at `specs/cards/{stock_shortage,debt_rent}.spec.yaml` (supplier spec unchanged from Phase 3). 49 new template tests (16 supplierReliability + 17 stockShortage + 16 debtRent), 6 new `runAllGates` blocks (three template-level + three ad-hoc choice-pool), three new `Template 3a/3b/3c` blocks in `tests/cards/templates.test.ts` and three new voice blocks in `tests/cards/templates.voice.test.ts`, six new sampler families in `tests/cards/compose/gates/samplers.ts` (actor-perturbation for supplier mirroring Phase 7/8; state-perturbation for stock/debt with a pre-computed 12-entry perturbation table per template covering rising pressures × memories × tone tags × severity). No Movement I loopback — Phase 1's signal surface plus the four primitives (`pressureRising`, `memoryPresent`, `repeatCount`, `hasTag`, `severityAtLeast`) drove every sim-backed slot across all three templates. Two Phase-3 spec snippets were reworded during emission to satisfy the sim-coherence gate's `\bagain\b` unbacked-history detector (`rxn_warm` "see you again" → "see you here"); supplier title snippets were tightened to ≤ 3 words so 3-word supplier display names + colon + snippet fit the legacy 6-word title budget. Full suite 2101/2101 across 168 files. Tracked as ISSUE-104.

**Phase 161 (Legible Surface arc, Phase 16, The Legibility Gate): done.** Movement VIII centerpiece. Closes the cross-template integration check the arc was pointing at since Movement V: per-template `runAllGates` runs the seven always-on framework gates (coverage, specificity, voiceBounds, simCoherence, determinism, diversity, dedupe) for every template's own pool walk — and (this clause originally overstated the rest; corrected by Complete Surface arc, Phase 1 / ISSUE-136 / phase 168) the two render-path gates `previewVariety` + `choiceDistinctness` are now also wired into every migrated template's per-template walk via `tests/cards/compose/gates/fullGateHarness.ts`, while `reportLegibility` stays report-section-only (its magnitude band lives in a routing tag card snippets never gate on). At Phase 161 itself, though, nothing surveyed the 20 migrated compositional templates AT ONCE and asserted both Q1 (establishing-line salience) + Q2 (preview legibility + choice distinctness) hold across the registry. Pre-Phase-161 a new compositional template that forgot to wire `previewVariety.legibility` or `choiceDistinctness` into its `runAllGates` config would silently regress; a pool whose `saliencePolicy` slot let the gradient suppress a covering snippet would pass per-template tests because no per-template gate audits salience coverage cross-template. New sibling gate `checkLegibility` at `src/cards/compose/gates/legibility.ts`, exported through `gates/index.ts` alongside the framework gates + the four post-framework gates + the `crossSituation` sibling. **Not in `runAllGates`** — like `crossSituation`, multi-template by construction (iterates a registry of 20 migrated templates, emits one composite report); the runner stays template-scoped. **Three structural rules, five violation reasons** in the frozen `LEGIBILITY_REASONS` tuple: **Q1 — `establishing_off_salient`**: for every (sample, slot-with-`saliencePolicy`) where `resolveSalientReads` returns ≥1 meaningful fact AND a snippet fired, fail when (a) the fired snippet's `scoreCandidateSalience.index === Infinity` AND (b) the pool contains a state-matching covering snippet the gradient/specificity layer suppressed. The "meaningful" filter excludes mid-band signal reads (extremity ≤ 1) — Movement-VI pools intentionally leave mid-band unauthored as the default-is-the-fact case; pressures / memories / repeats / hasTag / severity are binary so always count. The "state-matching covering snippet exists in the pool" check distinguishes Phase-1 failure mode (gradient over-rode salience) from authoring decisions (pool simply doesn't author for a state combination because the seed type guarantees it'd never occur — e.g. `regularComplaint` never sees `irritation=low` because the complaint seed type fires only at irritation > 60). **Q2 — preview legibility composed in-place**: for every sample, render the production `CardView` via `template.toCardView(assembleSlots(slots, seed, state), seed, state)` so the legible cap, voice composition, and choice ordering are exactly what the player would see. Walk each `CardChoice`, re-derive the per-line `EffectPreview` by mirroring `composeChoicesFromSeed`'s Phase-147 inaction routing (`immediate.length === 0 && delayed.length > 0`), then check three sub-rules: `preview_magnitude_missing` (an effect with `magnitudeBand` + `direction` renders without a `MAGNITUDE_LEXICON[direction][band]` token AND is not the sim fallback `effect.readable`); `preview_cost_unsurfaced` (a choice carries any negative-direction `coin` effect and no rendered preview line contains a `DEFAULT_TARGET_KIND_KEYWORDS.coin` token); `preview_inaction_blank` (a choice renders zero preview lines — only fires when the profile has no immediate AND no delayed effects, since the Phase-147 inaction wiring sources from delayed when immediate is empty). **Q2b — `choice_label_collision`**: canonical-equal labels (via `canonicaliseText` from `gates/dedupe.ts`) across distinct `(slotId, verb, targetId)` triples. Mirrors `choiceDistinctness.choice_label_collision` but exercises the cross-template registry. **20-template registry** at `tests/cards/compose/gates/legibilityHarness.ts` — `LEGIBILITY_SITUATIONS` listing every entry in `REQUIRED_CARDS` minus `fallbackCard`. Each entry's `buildSamples()` returns the existing per-template `build*DeterminismSamples()` factory output from `samplers.ts` (10-19 samples each, covering the state-perturbation matrix each cluster needs). `DeterminismSample` shape (`{ seed, state }`) is structurally identical to `LegibilitySample`. `fallbackCard` excluded by design (no `saliencePolicy` slots; uses `buildChoicesFromSeed` — no label/preview pools). **Composite report** `LegibilityReport = GateReport & { observed: { situations: LegibilitySituationObservation[] } }` with per-situation tallies (`samplesEvaluated`, `salienceReadsResolved`, `salienceReadsCovered`, `choiceSetSizesAfterCap`, `magnitudeChecksRun/Failed`, `costSurfacingChecksRun/Failed`, `inactionBlankCount`, `labelCollisionsCount`) — surfaced for diagnostics so tests can sanity-check coverage. **10 new tests** in `tests/cards/compose/gates/legibility.test.ts`: 5 live tests (pass across 20 situations; `salienceReadsResolved > 0` for ≥15 of 20 templates; inaction-preservation; determinism; frozen-tuple export) + 5 failure-fixture tests (one per `LEGIBILITY_REASONS` entry). The `establishing_off_salient` fixture demonstrates the real Phase-1 failure mode: pool has `est_fallback` (spec 0), `est_covering_memory` (spec 1, gated on `memoryPresent supplier` — covers a salient read), AND `est_noncovering_high` (spec 2, gated on `severityAtLeast 30 + severityBelow 60` — matches state but covers no salient read). Sample installs a `supplier`-tagged memory so the salient memory read resolves; the gradient picks the spec-2 non-covering snippet; the gate's pool walk finds the suppressed `est_covering_memory` and fires. **No sim-side changes**, **no new condition primitives**, **no new gates beyond `checkLegibility` itself**, **no change to `runAllGates` shape**: `pickSnippetTrace`, `resolveSalientReads`, `scoreCandidateSalience`, `composeChoicesFromSeed`, `applyLegibleChoiceCap`, `MAGNITUDE_LEXICON`, `DEFAULT_TARGET_KIND_KEYWORDS`, `canonicaliseText`, `lineCarriesMagnitude`, `evalCondition`, `assembleSlots` all reused unchanged. Pure: same `LegibilityConfig` ⇒ same report. Full suite green at 3063/3063 across 222 files (+10 vs the post-Phase-15 baseline of 3053). Tracked as ISSUE-129.

**Phase 168 (Complete Surface arc, Phase 1, The Gate-Wiring Contract): done.** Movement I foundation — makes it structurally impossible for a card template to ship under-gated. Closes audit finding #2: `previewVariety` (Phase 144) and `choiceDistinctness` (Phase 148) were *opt-in* `runAllGates` config blocks configured for **zero / one-synthetic** real templates, so every migrated template's per-template walk ran only the seven always-on framework gates; and the cross-sim harness registries (`legibilityHarness`, `crossSituationHarness`) were hand-maintained lists with no completeness check — a template added to `REQUIRED_CARDS` but forgotten in a harness silently lost coverage. **New harness** `tests/cards/compose/gates/fullGateHarness.ts`: `FULL_GATE_SITUATIONS` (the 20 migrated templates = `REQUIRED_CARDS` minus `fallbackCard`), each carrying `{ templateId, template, labelPool, previewPool, buildSamples }`. `buildSamples` reuses the per-template `build*DeterminismSamples()` factories from `samplers.ts` (the same `(seed, state)` source the legibility harness uses). Two generic sampler builders — `previewVarietySamplerFor` / `choiceDistinctnessSamplerFor` — derive the multi-choice render from the seed's own `responseSlots` + `consequenceProfiles` (sourcing preview effects from `immediateEffects`, or `delayedEffects` when immediate is empty — the Phase-147 inaction carve-out), apply the production `applyLegibleChoiceCap` for choice distinctness, and mirror production's uniform `maxPreview: 2` (exported as `PRODUCTION_MAX_PREVIEW`; verified across all 20 `src/cards/templates/*`). **New test** `fullGateWiring.test.ts`: a data-driven loop running `runAllGates(template, { …, previewVariety, choiceDistinctness })` per template, asserting both gates ran (`skipped === false`) and the full nine-gate aggregate passes. **New test** `harnessCompleteness.test.ts`: derives `REQUIRED_IDS` from `REQUIRED_CARDS` and fails if any non-fallback template is absent from `FULL_GATE_SITUATIONS`, `LEGIBILITY_SITUATIONS`, or `cardRegistry.all()` (the faithfulness walk's candidate set); derives the **actor-voiced** subset structurally (a template whose body/title slot pools carry any `voiceAxis` / `verbalTic` condition — yields exactly the 11 the crossSituation harness lists) and asserts each appears in `CROSS_SITUATION_ACTOR_KINDS`; asserts no stale registry entries; records the `reportLegibility` card-template exclusion as data (`CARD_TEMPLATE_EXCLUDED_GATES`). **No pool changes needed** — the one gate that fired (`area_atmosphere` within-card preview collapse) was a sampler artifact from a third preview line production never renders; mirroring `maxPreview: 2` resolved it (the cross-sim legibility gate already validated the same render paths, so no real defect was hiding behind the opt-in switch). The inaccurate CLAUDE.md Phase-161 claim is corrected in place. Cross-sim harnesses stay multi-template siblings; this phase makes their completeness checkable. 26 new tests (20 full-gate-wiring + 6 completeness); full suite green at 3187/3187 across 230 files (+26 vs the post-Phase-167 baseline of 3161), typecheck clean. Tracked as ISSUE-136.

**Phase 169 (Complete Surface arc, Phase 2, drinkOrder Parity): done.** Movement I, the one un-migrated flagship card. `drinkOrderCard` (original Phase-C compositional card, Living Cast phase 123) was the last template never brought to parity: its body builder spliced raw `seed.textIngredients.recentContext[0]` straight in as the last line (`drinkOrder.ts:153-154`) — the un-composed, un-gated, un-state-checked fragment the Voiced and Faithful arcs killed everywhere else — and it had no sim-backed establishing slot, no `saliencePolicy`, no salience-table read; it opened on the voiced flavor `order_line` while the other nineteen open on a salient fact. The companion `phase127.simBackedHookSignal.test.ts` guarded its three primitive checks behind a dead `it.skip` (the case where no starter regulars are seeded — but `createInitialTavernState()` always seeds eleven, all with castAttributes), so the render-level assertion Voiced Surface Phase 1 promised "in principle" never landed. **New `establishing_line` slot** on `drinkOrderTemplate` (`role: 'utterance'`, `wordBudget: 14`, `claimMode: 'sim_backed'`, `saliencePolicy: 'multi'`, `multiFactJoin: ' — '`), mirroring the staffAside / supplier_reliability shape — lands first in the body, before the voiced `order_line`. **New matrix pool** at `src/cards/compose/pools/drinkOrder/establishingLine.ts` (`drinkOrderEstablishingLinePool`, 16 snippets) keyed on the shared `regular_customer` salience table (irritation × loyalty bands, the `regular_customer_loss` pressure, the grudge / customer / warning memories, the `regular` repeat): one unconditional fallback + single-condition rungs + the four reachable irritation × loyalty corners + two orthogonal signal × (pressure | memory) top rungs. **Reachability discipline (arc principle 2):** drink_order is the MILD branch of `regular_customer` (`type: 'relationship_test'`, emitted only when `irritation ≤ 60` per `expandedSeedGenerators.ts:1066`); with `regular.irritation` band thresholds `[40, 70]` the seed reaches irritation bands `low` and `mid` only — never `high` — so the pool authors only the reachable face of the shared cube (irritation ∈ {low, mid} × loyalty ∈ {low, mid, high}); the high-irritation row stays `regularComplaint`'s complaint branch, and the `mid × mid` interior stays on the unconditional fallback (Phase-149 "leave the unremarkable centre to the fallback" precedent). The full-cube completeness pass for this family is Movement II, Phase 6 (ISSUE-141), which owns the reachable-cell enumeration + `unreachableCells` allowlist; this phase brings the card to *parity*, not to machine-checked completeness. **Raw splice deleted**: `buildDrinkOrderBody` no longer reads `seed.textIngredients`; the body is fully composed `[establishing_line, order_line, manner_note?]`. **Skipped test upgraded**: the dead `it.skip` guard removed; three new render-level assertions prove `resolveSalientReads` returns the two signal reads for the starter regular and `drinkOrderCard.render(...).body[0]` is the covering `est_low_irritation_high_loyalty` combo (the starter regular's irritation=low × loyalty=high standing), not the bare fallback, deterministically. **Gate wiring**: drinkOrder's `runAllGates` block gains an `establishing_line` diversity slot (`minDistinct: 1` — the voice-perturbation sampler doesn't vary signal state, matching the staffAside / regularComplaint config); drinkOrder is already in every cross-sim harness (legibility / crossSituation / fullGate) so the Phase-1 completeness test stays green. **Mechanical choice fields untouched** (verb / targetId / shape / per-effect kind / target / amount / tags) — the Faithful rule. Three body-index render tests in `templates.drinkOrder.test.ts` shifted (`order_line` is `body[1]` now, its companion `manner_note` `body[2]`). Spec `specs/cards/drink_order.spec.yaml` updated (the `sim_backed_hook` slot realised as the live `establishing_line` slot + a phase169 design record). Tracked as ISSUE-137.

**Phase 170 (Complete Surface arc, Phase 3, Salience Completeness & the Reachability Allowlist): done.** Movement I foundation — arms Movement II by closing the one tableless seed family and shipping the two data structures every matrix-fill phase (4–11) and the Phase-12 coverage gate read against. **Reads + scaffold only — no establishing content, no coverage gate, no condition-DSL change.** (1) **`policy_backlash` salience entry** added to `src/cards/compose/salience.ts` — the last `IssueSeedFamilyId` with an active generator (`generatePolicyBacklash`, `expandedSeedGenerators.ts:4495`, registered via `EXPANDED_SEED_GENERATORS` → `ALL_SEED_GENERATORS`) and no `SALIENCE_TABLES` entry. It routes to `fallbackCard` today (no live template), but the arc's coverage contract requires every active-generator family to declare its salient reads so the first dedicated policy_backlash card opens on a fact and the derived completeness test has no hole. **Narrator-voiced** (the policy `primaryActor` is `{ kind: 'other', id: 'policy:<id>' }` — no castAttributes, no banded signal), so the table follows the Phase-149 / 155 / 156 shape: `severity ≥ 70` crisis floor leads (the generator sets `severity = max(35, backlash.severity)`; no categorical what-is-this-about tag exists the way reputation_shift has), then the two embedded pressures `policy_backlash` (primary) / `faction_anger` (secondary; `expandedSeedGenerators.ts:4858`), then memory tags `warning` / `grudge` / `reversal` / `policy` mirroring the response-profile emissions (the punish path tags `grudge` at `:4819`, the repeal path `reversal` at `:4722`), then `repeat policy ≥ 3` as the deepest rung. No new `SalienceRead` kinds. (2) **Matrix-cell read** — new `src/cards/compose/matrixCells.ts`: `MAX_MATRIX_METERS = 3` (a 3-meter cube of 27 cells is the documented establishing-matrix ceiling, legible-surface Appendix A); `matrixMetersForFamily(family)` returns the salience table's `signal` reads in salience order capped at three (so `inspection`, which lists four signal reads, yields a 27-cell cube not 81), guarded against a typo'd unbanded signal via `BAND_THRESHOLDS` membership; `matrixCellKey` builds the stable self-describing key `"signalA=low|signalB=high"`; `enumerateMatrixCells(family)` is the cartesian product of `ALL_BAND_IDS` (low/mid/high) over the meters — `3^n` decision-distinct cells (n ≤ 3), deterministic (band order = `ALL_BAND_IDS`, meter order = table). Narrator-voiced families with no banded signal on the subject (policy_backlash, stock_shortage, debt_rent, reputation_shift, rumour_crisis, rival_tavern, monthly_review, seasonal_arc) enumerate to **zero cells** by design — their establishing line keys off pressure / memory / severity, which aren't band-enumerable. Pure, no closures, enumerable by the gate. (3) **Reachability allowlist scaffold** — new `src/cards/compose/unreachableCells.ts`: `UnreachableCell = { cellKey; reason }`, `UNREACHABLE_CELLS: Partial<Record<family, readonly UnreachableCell[]>>` **seeded empty**, `isCellAllowlisted`, and `validateUnreachableCells` (the real-cell invariant: every entry's `cellKey` must be an enumerable cell of its family + no duplicates — the guard that fails a typo'd Movement-II addition fast and named). **Seeded empty deliberately**: the arc's Phase-3 "Do not do" forbids guessing, the motivating audit named gaps only in aggregate ("~8 of 27"), and the doc's illustrative `rowdiness = low` cell is actually *selectable* (the violence picker takes `dangerous`-tagged low-rowdiness groups) so it is NOT unreachable and must not be seeded; each Movement-II cluster phase populates its family's entries after instrumenting that picker. All exported through `src/cards/compose/index.ts`. New `tests/cards/compose/phase170.salienceCompleteness.test.ts` (14 tests): derived completeness over `ALL_SEED_GENERATORS` (all 20 active families covered, fails naming any miss), policy_backlash render-resolution (severity floor surfaces at severity 82, drops below it) + no-new-kinds check, matrix-cell read (supplier 9-cell, culture 27-cell cube, inspection cap to 27, narrator → 0, key/band parallelism, determinism, no-unbanded-signal across all families), and the empty + validated allowlist. CLAUDE.md + ISSUE_TRACKER updated. Full suite + typecheck green. Tracked as ISSUE-138.

**Phase 171 (Complete Surface arc, Phase 4, Suppliers, Stock & Debt matrix fill): done.** Movement II's first content cluster — and the first application of the Phase-3 cell-coverage read (`enumerateMatrixCells`, ISSUE-138) to content. The deliverable is **making "filled" a machine fact** for the cluster, not authoring prose (the prose shipped in Legible Phase 4 / ISSUE-117). Under the Phase-3 read the cluster's only band-enumerable matrix is `supplier_relationship`'s **9-cell `supplier.reliability × supplier.relationship` cube**; `stock_shortage` and `debt_rent` are narrator-voiced (no banded subject signal — the stock subject is a stock item, the landlord is a `systemRef` per audit pass 1 §5.3) and enumerate to **0 matrix cells by design**, their establishing coverage being over binary salient facts (the `severity ≥ 70` floor, rising `stock_shortage` / `reputation_drift` / `debt` / `landlord` pressures, choice memories, the `rent_due_soon` calendar window, the multi-period repeat) already authored exhaustively in Phase 149 and exercised render-level in `phase149.exhaustiveMatrix.test.ts`. **Supplier 9-cell is already covered to the cell** by the Phase-149 pool — four hand-authored combo corners (`est_low_rel_low_rship` / `est_low_rel_high_rship` / `est_high_rel_low_rship` / `est_high_rel_high_rship`), four single-meter edge snippets (`est_low_reliability` / `est_high_reliability` / `est_low_relationship` / `est_high_relationship`, each firing on its one non-mid meter regardless of the other meter's band), and the mid × mid centre on `est_fallback`. This matches the Phase-161 legibility gate's "meaningful read" filter: a mid-band signal read is extremity 1 (the intentional default), so only the low/high (extremity-2) cells demand a covering snippet and the all-mid centre is the fallback's job. **Reachability — no allowlist entries.** Instrumenting `generateSupplierRelationship` (`expandedSeedGenerators.ts:1127`): it fires when `supplier_distrust + market_instability ≥ PRESSURE_THRESHOLD`, then ranks suppliers by `blame + memory*10 + reliabilityDeficit − recencyPenalty`. The score biases toward low reliability via `reliabilityDeficit` but does **not** floor it (a high-reliability supplier carrying blame/distrust memory can still be the pick) and `relationship` is unscored — so all nine reliability × relationship cells are reachable, and `UNREACHABLE_CELLS` stays **empty** for the cluster (the arc's rule: allowlist only proven-unreachable cells, never on a guess). **New test** `tests/cards/compose/phase171.matrixCoverage.test.ts` (16 tests) *derives* the supplier cells from `enumerateMatrixCells` (not a hard-coded 9) and, per cell, builds a clean state with the starter supplier pinned to the cell's band-representative values (20/55/85 against the `[40, 70]` thresholds), resolves the salient reads with the same mid-band filter the legibility gate uses, and asserts the establishing slot's `pickSnippetTrace` fires a snippet whose `scoreCandidateSalience` index is finite (a covering snippet, not the bare fallback) for every meaningful cell and the fallback for the all-mid centre; plus the four-corner render check (`body[0]` ≠ fallback), the `stock_shortage` / `debt_rent` 0-cell + salient-binary-fact assertions, and `validateUnreachableCells()` clean with no cluster entries. **No new snippets, no picker widening, no allowlist entries, no coverage gate** (the standing `checkMatrixCoverage` gate is Phase 12 / ISSUE-147 — this phase ships the per-cluster *proof* the gate will later generalise). The cluster's three templates are already in `FULL_GATE_SITUATIONS` (Phase 168 nine-gate walk), `LEGIBILITY_SITUATIONS`, and the faithfulness walk; no harness edits needed. Full suite + typecheck green. Tracked as ISSUE-139.

**Phase 159 (Legible Surface arc, Phase 14, Operational Previews + Pressure & Delayed/Uncertain framing): done.** Third and final Movement-VII per-meter authoring phase, mirrors Phase 158's shape across the three operational meters (`staff`, `area`, `pressure`) plus the **delayed/uncertain (inaction) framing** that closes Phase 147's `inactionPreview` pilot pattern into the shared base. Pre-Phase-159 the three blocks in `src/cards/compose/pools/_shared/effectPreviewBase.ts` gated only on `(effectTargetKind × effectDirection)` with no magnitude vocabulary: staff -10 (medium) and -3 (small) both rendered "the rota would wear thin tonight"; area damage +6 (tiny accrual) and condition -8 (small decay) both rendered "the corner would slip further"; pressure +6 small and +15 medium both rendered "the meter would climb a notch". And every inaction profile outside `areaAtmosphere` (14 `immediateEffects: []` profiles across `issueSeedGenerators.ts` + `expandedSeedGenerators.ts` — stock_shortage, maintenance, staff_burnout, debt, inspection, food_safety, customer_complaint, regular_loss, rival, rumour, …) emits delayed positive pressure as its consequence but rendered the same active-choice base prose on the inaction path. Phase 14 recalibrates the three blocks to the 3-condition `direction × magnitudeBand` shape Phases 12 / 13 used (~40 new active-choice snippets replacing 14 existing kind+direction entries) and adds a new specificity-4 **inaction-pressure block** at the end of `narratorEffectPreviewBase()` (6 snippets gated on `inactionPreview: true` + positive direction × small/medium/large) so every family inherits "what *not* acting costs" framing without per-template authoring. Pressure direction semantics are encoded via verb palette: positive = rising = bad ("build / mount / climb / creep / press / thicken"); negative = relief = good ("settle / ease / loosen / fall back"). Inaction-block snippets carry temporal-claim verbs ("would keep …" / "would mount unchecked" / "would build with every hour") that frame inaction cost without tripping the sim-coherence `\bagain\b` / `\byesterday\b` detectors. Production cells (per the audit at `docs/plans/phase-159-operational-previews.md`) carry 2-3 snippets per high-traffic cell for FNV spread (staff pos small, pressure pos/neg small, pressure pos medium); cells the sim doesn't emit get a single optimistic snippet (staff tiny, area large, pressure tiny/large). **Axis-neutrality decision** — pressure effects encode the family (`pressure:landlord`, `pressure:food_safety`, …) in the target string, not in tags; Phase 14 stays at flat-data conditions and authors axis-neutral pressure snippets calibrated on `direction × band` only. Per-pressure-family specificity is a deliberate future loopback candidate, same shape as Phase 13's per-reputation-axis decision. **Four snippet rewordings during authoring**: `shared_preview_area_neg_tiny_a` "a hair of wear would settle on the floor" collided at 0.850 with `shared_preview_culture_pos_tiny_a` "a hair of ease would settle on the folk" — reworded to "would creep across the floor" to break the "a hair of X would settle on the Y" template; three staff snippets (`_neg_medium_a` / `_pos_tiny_a` / `_pos_medium_a`) all contained "the cook line" which tripped the simCoherence `unbacked_role_claim` detector (`\b(your|the)\s+(cook|cleaner|server|guard|bouncer)\b`) — reworded to "kitchen crew" / "rota tonight" / "kitchen". **No sim-side changes**, **no new gates**, **no new condition primitives**: `composeChoicesFromSeed`, `MAGNITUDE_LEXICON`, the legibility rules, the `inactionPreview` primitive, and `effectTag` all reused unchanged. No per-template pool harmonisation needed — the new band-keyed shared snippets out-rank the existing generic per-template variants on the FNV tie-break for `(targetKind, direction, band)` matches at the same specificity; the existing voice-axis-gated specificity-3 snippets stay (different condition shape, no tie). The `areaAtmosphere` Phase-147 inaction pilot snippets (`pre_inact_pressure_pos_medium`, `pre_inact_area_neg_tiny`, `pre_inact_area_pos_tiny`) stay in place — they gate on `effectTargetKind: ['area']` and out-rank the shared pressure-only inaction snippets for area effects. **57 new tests**: 43 cell-level units in `tests/cards/compose/phase159.operationalPreviews.test.ts` (staff 8 cells × keyword+magnitude, area 7 cells, pressure active-choice 8 cells, pressure inaction 3 cells × keyword+magnitude+threat-verb checks, inaction-only direction gate test, 4 inaction-routing distinction tests proving active vs inaction paths render different snippets for the same pressure effect, 5 determinism end-to-end checks, 3 Phase 14 retirement guards, 3 magnitude lexicon parity checks) plus 14 cluster live tests in `tests/cards/compose/gates/previewVariety.live.test.ts` (staffAside, staffBurnout, maintenance + inaction, areaAtmosphere + inaction, foodSafety + inaction, inspection + inaction, debtRent inaction, stockShortage inaction, customerComplaint inaction, violence, factionRequest + inaction, cultureConflict inaction, seasonalArc inaction, monthlyReview inaction). **Movement VII is complete after this phase** — three per-meter passes covering coin/stock (economic, Phase 157), customer/cohort/reputation/supplier/faction/culture (social, Phase 158), and staff/area/pressure + inaction (operational, Phase 159). Tracked as ISSUE-127.

**Phase 158 (Legible Surface arc, Phase 13, Social Previews): done.** Second Movement-VII per-meter authoring phase, mirrors Phase 157's shape across the six social meters (`customer`, `cohort`, `reputation`, `supplier`, `faction`, `culture`). Pre-Phase-158 those blocks in `src/cards/compose/pools/_shared/effectPreviewBase.ts:417-606` gated only on `(effectTargetKind × effectDirection)` with no magnitude vocabulary — `customers.merchants.patronage +12` (medium) and `+4` (small) both rendered "the patron would warm a measure", `reputation.respectable -8` (small) and `-3` (tiny) both rendered "word would turn against the name", `factions.X.relationship -20` (large) read identically to a `-8` (small) snub. Phase 13 recalibrates all six social blocks to the 3-condition `direction × magnitudeBand` shape Phase 12 used — ~60 new social-meter snippets replacing 12 existing kind+direction snippets. Production-emitted cells (per the audit at `docs/plans/phase-158-social-previews.md`) carry 2-3 snippets per high-traffic cell for FNV spread (customer small/medium, reputation small dominant on both sides, faction medium / pos-small, culture medium); cells the sim doesn't emit today get a single optimistic snippet (cohort across the board — cohort effects are exclusively `cause` writes, not `state_change`; customer tiny / pos-large; reputation large; faction tiny; culture tiny/large). Every banded snippet contains a `MAGNITUDE_LEXICON[direction][band]` token (passes `requireMagnitude`) and a `DEFAULT_TARGET_KIND_KEYWORDS` token for its targetKind. **Axis-neutrality decision** — reputation effects encode the axis in the target string (`reputation.respectable` / `dangerous` / `cheap`), not in tags or a condition primitive; Phase 13 stays at flat-data conditions and authors axis-neutral snippets calibrated on `direction × band` only. Per-axis specificity is a deliberate future loopback candidate. **Seven snippet rewordings during authoring** to clear the dedupe gate at the 0.85 similarity threshold — the bigger risk in Phase 13 vs Phase 12 was template-reuse across meters (coin/stock are mechanically distinct enough that vocabulary doesn't repeat; social meters all describe relationship-style movements with high natural overlap: "cool by a step" / "warm by a step" / "pull back by a real slip" got reused across customer/faction/culture and tripped the gate). Fixed by injecting more distinct verbs ("knit" / "shutter" / "harden" / "chill") and varying sentence structure (subject-leads vs magnitude-leads). **No sim-side changes**, **no new gates**, **no new condition primitives**: `composeChoicesFromSeed`, `MAGNITUDE_LEXICON`, the legibility rules, the `inactionPreview` primitive, and `effectTag` all reused unchanged. Per-template harmonisation was light — only `supplierReliability/effectPreview.ts` carries substantial pre-Phase-158 social snippets (`pre_leg_supplier_*` + `pre_leg_reputation_*` at 3-condition specificity); base snippets authored with distinct vocabulary so dedupe stays below 0.85 and FNV tie-break spreads renders between base and template variants. **Pressure / staff / area target meter previews are deliberately deferred to Phase 14** (Operational Previews) which owns the operational cluster + rising-pressure / delayed-effect framing as a coherent meter family. **72 new tests**: 63 cell-level units in `tests/cards/compose/phase158.socialPreviews.test.ts` (customer / reputation / supplier / faction / culture / cohort 8 cells each × keyword+magnitude, 7 determinism end-to-end checks including axis-neutral reputation cross-axis equivalence, plus 6 retirement-guard tests asserting every social-meter snippet now carries a magnitudeBand condition except explicit neutral fallthroughs) plus 9 cluster live tests in `tests/cards/compose/gates/previewVariety.live.test.ts` (regularComplaint, customerComplaint, factionRequest, cultureConflict, reputationShift, rumourCrisis, supplierReliability, violence, monthlyReview). Tracked as ISSUE-126.

**Phase 148 (Legible Surface arc, Phase 3, Choice Distinctness Gate & Legible Choice-Set Cap): done.** Closes Movement V's three-phase prerequisite (salience → preview-legibility → choice-distinctness). Fixes two defects: (a) the supplier `choiceLabel.ts` pool gated only on `(responseVerb, voiceAxis)`, so a terse-axis supplier collapsed `negotiate_supplier` and `supplier_exclusivity_deal` to the identical *"Cut the terms shorter"* — distinct mechanical slots, identical rendered label; (b) the supplier seed's eleven response slots rendered as an eleven-choice wall that buried the most decision-relevant options. Four additive layers, mechanical truth (verbs / targets / profiles / slot identity) untouched everywhere. (1) Thirteenth flat-data condition primitive `responseSlot { anyOf }` in `src/cards/compose/{types,conditions}.ts` mirroring `responseVerb` / `responseShape` — reads `ctx.currentResponseSlot?.id`, returns false outside choice iteration. (2) Ninth structural gate `checkChoiceDistinctness` at `src/cards/compose/gates/choiceDistinctness.ts`, sibling to `previewVariety`. Pure function over `(sampler, config) → GateReport & { observed }`: simulates a full multi-choice card render via `pickSnippet` (same path `composeChoicesFromSeed` uses), fails on `choice_label_collision` (canonical-form-equal labels across distinct `(slotId, verb, targetId)` triples via `canonicaliseText`), warns on `choice_preview_near_collision` (joined-preview Levenshtein similarity ≥ 0.85). Wired through `runAllGates` as the ninth slot in `AllGatesConfig.choiceDistinctness?` with `ChoiceDistinctnessReportEntry` populated when configured / `skipped: true` otherwise. (3) New optional `warnings?: GateViolation[]` field on `GateReport` (parallel to `violations`; never affects `pass`). Invariant preserved across all nine gates: `report.pass === (report.violations.length === 0)`. The eight pre-Phase-148 gates leave `warnings` undefined; existing assertions like `report.violations.length === 0` keep meaning exactly what they always have. (4) Pure helper `applyLegibleChoiceCap(seed, slots, profileFor, maxChoices)` in `src/cards/cardHelpers.ts` — the legible choice-set cap as a *presentation* policy, no sim slot changes. Scores every slot by salience against `SALIENCE_TABLES[seed.family]` (Phase 1 / ISSUE-114): min table index across the slot's `immediateEffects` (or `delayedEffects` when immediate is empty — Phase-147 carve-out). `effect.targetKind === 'pressure'` + `target === 'pressure:<id>'` matches a `pressure` read; `targetKind` matching a `signal` read's SignalId prefix kind matches; `effect.tags` overlap matches `memory` / `repeat` reads. Sorts by `(score asc, originalIndex asc)` — ties preserve seed-author order. Caps to `DEFAULT_LEGIBLE_CHOICE_CAP = 6` (no-op for the nineteen other compositional templates whose seeds emit ≤ 6 slots; cuts the supplier 11-slot case to 6). Always preserves the inaction slot (verb `'ignore'` OR empty `immediateEffects` with non-empty `delayedEffects`) — appended past the cap if salience would otherwise drop it. Threaded through `composeChoicesFromSeed` via new optional `ComposeChoicesOptions.maxChoices?`; every template calls through unchanged. (5) `src/cards/compose/pools/supplierReliability/choiceLabel.ts` extended with eight slot-distinct snippets at `specificity: 2` (and four voice-axis-paired variants at `specificity: 3`) gated on the new `responseSlot` primitive for the four same-first-verb collision sets: `supplier_exclusivity_deal` / `place_standing_order` / `split_orders` / `investigate_suspicious_goods`. Explicit specificity overrides ensure the slot-id-keyed snippets out-rank the existing `responseVerb + voiceAxis` (2-condition) snippets for the colliding peer slots while letting the older verb-keyed snippets keep firing for non-colliding same-verb slots. 21 new tests at `tests/cards/compose/phase148.choiceDistinctness.test.ts` (Layer A condition primitive ×3, Layer B gate fail/pass/warn/determinism/wiring ×9, Layer C cap policy + integration ×9) plus 4 new live tests in `tests/cards/templates.supplierReliability.test.ts` exercising a real eleven-slot supplier seed (cap to 6, distinct labels across same-verb pairs, mechanical-mapping preservation, re-render stability). Full suite 2649/2649 across 207 files (+25 vs the post-Phase-147 baseline). Tracked as ISSUE-116.

**Phase 147 (Legible Surface arc, Phase 2, Preview Legibility Contract & Consequence of Inaction): done.** Continues the Legible Surface arc from Phase 1's signal salience read. Voicing solved the *prose* problem; this phase tackles the second half of the *legibility* problem on the choice side. Pre-Phase-147 previews failed two ways: (a) voiced lines dropped the magnitude band the sim already classified — "shelves would thin a measure" landed meter+direction but no calibrated magnitude, and `small` read inconsistently as "a notch / a measure / a touch / a hand" across pools, leaving a coin-spending choice indistinguishable from a coin-gaining one; (b) the inaction option rendered blank because `composeChoicesFromSeed` at `cardHelpers.ts:213` mapped `profile?.immediateEffects ?? []` to preview lines, and 14 inaction profiles in `issueSeedGenerators.ts` + `expandedSeedGenerators.ts` (e.g. `ignore_area_problem_profile` at `expandedSeedGenerators.ts:2692`) declare `immediateEffects: []` with all consequences in `delayedEffects`. **Magnitude lexicon.** New data file `src/cards/compose/magnitudeLexicon.ts` ships `MAGNITUDE_LEXICON: Record<EffectDirection, Record<EffectMagnitudeBand, readonly string[]>>` — `positive` / `negative` share vocabulary at `tiny` + `small` (verb carries direction: "rise a notch" vs "fall a notch") and diverge at `medium` + `large` (the magnitude word itself reads directional: "a clear lift" vs "a clear drop"). Plus a `lineCarriesMagnitude` helper for case-insensitive substring matching. **Inaction wiring.** `composeChoicesFromSeed` now routes preview composition through `delayedEffects` when `immediateEffects` is empty, threading a new optional `ConditionContext.inactionPreview: true` so pools can author "what not acting costs" variants. Twelfth flat-data condition primitive `inactionPreview { value: boolean }` lands in `src/cards/compose/types.ts` + `conditions.ts`, graceful-degrading to `false` when the ctx field is absent (body / title slot evaluation unaffected). **Three opt-in legibility rules** on the existing Phase-145 `checkPreviewVariety` gate via new `PreviewLegibilityRule` config nested alongside the specificity rule: `requireMagnitude` (every banded line carries a `MAGNITUDE_LEXICON[direction][band]` token; sim fallthrough always counts), `requireCostSurfacing` (any choice carrying a negative-direction coin effect must surface a coin keyword), `forbidInactionBlank` (every choice renders ≥1 line). Three new violation reasons `preview_magnitude_missing` / `preview_cost_unsurfaced` / `preview_inaction_blank` appended to the frozen `PREVIEW_VARIETY_REASONS` tuple. New observation fields `magnitudeRatio` / `costSurfacingRatio` / `inactionBlankCount` surface rule outcomes. `PreviewVarietyChoice` gains optional `inactionPreview?: boolean` so the gate's render loop threads the new ctx. **Two pilot pools** prove the contract: `supplierReliability/effectPreview.ts` adds 17 magnitude-bearing snippets at specificity 3 (`effectTargetKind × effectDirection × effectMagnitudeBand`) covering the cells supplier seeds emit per `expandedSeedGenerators.ts` (coin -15 small through -30 medium; supplier ±3 tiny through ±20 large; pressure ±5 small through ±12 medium; reputation -8 small) with explicit till/silver/purse on every coin cell; `areaAtmosphere/effectPreview.ts` adds 12 magnitude-bearing snippets covering area / coin / pressure / reputation cells PLUS 3 inaction-specific snippets at specificity 4 gated on `inactionPreview: true` sourced from the `ignore_area_problem_profile`'s delayed effects. Rewording during authoring: 3 snippets had "a clear step" → "a real step" (the only `MAGNITUDE_LEXICON.positive.medium` token that matches); 1 dedupe fix in supplier ("the merchant would cool a hair" → "trust would dip a hair with the supplier" — similarity 0.867 against the warm variant); 1 dedupe fix in area (canonical equality with existing `pre_pressure_ease`). The shared `_shared/effectPreviewBase.ts` is untouched in this phase — the pilots out-rank it at specificity 3+ for matching effects; Movement VII's per-meter authoring will recalibrate the base for system-wide impact. 35 new tests (9 magnitude-lexicon + 11 inaction-primitive/wiring + 8 legibility-rule fixtures + 3 pilot-live + 4 areaAtmosphere-inaction-headline). Full suite 2624/2624 across 206 files (+35 vs the post-Phase-146 baseline). Tracked as ISSUE-115.

**Phase 145 (Voiced Surface arc, Phase 18, Effect-Preview Specificity Restoration): done (second playtest iteration).** After Phase 144 fixed within-card preview collapse, playtest still read as generic: card lines varied across choices but said nothing about what changed ("the rota notes it quietly" / "the bar absorbs the change" / "the count moves a touch") even when the underlying effect was `+15 coin` or `-25 cleanliness`. Root cause: per-effect snippet conditions exposed only `effectKind` and `effectTag` — `effect.target` and `effect.amount` were invisible to the snippet pools, so Phase-144's added kind-only base-rung snippets were *necessarily* generic. The sim-emitted `effect.readable` ("Earn coin from sales", "Kitchen cleaner", "Discard mushrooms") is the fallback at `src/cards/cardHelpers.ts:227`, but Phase 144 made it almost never reachable. **Sim-side metadata.** Extended `EffectPreview` at `src/sim/core/effect.ts` with three additive optional fields (`targetKind`, `direction`, `magnitudeBand`) classified by the `effect()` constructor at `src/sim/modules/issues/generatorHelpers.ts:51` — the single choke point through which all ~240 effect emissions flow. New types `EffectTargetKind` (16 variants: coin / stock / area / customer / staff / pressure / memory / reputation / cohort / supplier / faction / culture / arc / attribution / global / other), `EffectDirection`, `EffectMagnitudeBand` (`tiny|small|medium|large`). Per-targetKind cutoff table `MAGNITUDE_BAND_CUTOFFS` centralises what "small" means per meter family (coin 5/20/50, pressure 5/10/20, staff 3/8/15, etc.). **Three new flat-data condition primitives** in `src/cards/compose/types.ts` and `conditions.ts`: `effectTargetKind { anyOf }`, `effectDirection { sign }`, `effectMagnitudeBand { anyOf }`. All graceful-degrade to `false` when `ctx.currentEffect` or the classified field is absent — same shape as the Phase-6 `effectKind` / `effectTag` arms. **Shared narrator base.** New `src/cards/compose/pools/_shared/effectPreviewBase.ts` exports `narratorEffectPreviewBase(): Snippet[]` — 47 snippets covering the targetKind × direction grid in narrator register (ids prefixed `shared_preview_`). High-traffic targetKinds (`staff`, `customer`) carry 2-3 snippets per direction so the FNV tie-break at `pickSnippet` has multiple equal-specificity candidates to spread across a multi-effect card render. Each shared snippet contains a discriminating keyword for its targetKind (passes the new specificity rule), stays under the 10-word effect_preview budget, and avoids actor-role nouns (sim-coherence `role_claim` stays quiet). Merged into all 19 actor / narrator template pools (`fallback` excluded — no effect previews on choices) via `[...narratorEffectPreviewBase(), ...own]`. Four rewordings needed during authoring to clear sim-coherence (`\bagain\b` x2) and dedupe (canonical equality with template-specific Phase-144 snippets x2). **Gate extension, no 9th gate.** `src/cards/compose/gates/previewVariety.ts` extended with `PreviewSpecificityRule { minSpecificityRatio?, targetKindKeywords? }` nested inside the existing `PreviewVarietyConfig`; third violation reason `'preview_specificity_low'` added to `PREVIEW_VARIETY_REASONS`. `DEFAULT_TARGET_KIND_KEYWORDS` ships next to the gate (e.g. `coin → ['coin','till','purse','silver',...]`, `stock → ['shelf','shelves','stock','stores','barrel',...]`). A rendered line counts as "specific" when (a) it is the verbatim `effect.readable` — sim authority always counts; (b) it contains any keyword from the table for its effect's `targetKind`; or (c) the effect has no classifiable `targetKind`. The rule is **opt-in** — omitting the `specificity` config preserves Phase-144 behaviour. Default `minSpecificityRatio: 0.7`. Specificity check runs inside the existing `renderPreviewLines` loop — no new sampler API. **Tests.** 80 new classifier tests at `tests/sim/phase145.effectClassification.test.ts` (every observed target prefix + magnitude cutoff + direction sign rule, asserting no real sim-emitted target falls into `'other'`). 14 new condition-primitive tests at `tests/cards/compose/phase145.effectConditions.test.ts`. Four new fixture cases on `previewVariety.test.ts` (generic-varied fails specificity; specific pool passes; sim-fallback counts as specific; rule is opt-in). Three new live-suite assertions on `previewVariety.live.test.ts` covering staffAside / areaAtmosphere / stockShortage / seasonalArc / inspection (the Phase-144 screenshot-defect templates) — pre-existing live tests updated to use the real `effect()` constructor so `targetKind` lands on test fixtures. The runAllGates suite covers all 19 templates structurally; all 41 cases pass after pool injection. **Out of scope** (deferred to future Phase-18 iterations): voice-axis-gated specificity-3+ snippets per template (playtest-driven authoring, not bulk pre-author), YAML specs at `specs/cards/` (still lag in-repo pool authoring per the Phase-4 authoring loop), renderer-side per-effect amount/direction badges, a new coverage gate that walks reachable consequence profiles to assert no `targetKind: 'other'`. Tracked as ISSUE-113.

**Phase 144 (Voiced Surface arc, Phase 18, Deepening, Pruning & Voice-Selection Repair): done (first playtest iteration).** Fixes the visible defect set surfaced by the screenshots: Mira's `staff_identity` card showed "the kitchen keeps its quiet drumbeat" repeated across all 12 choices' preview lines; `area_atmosphere`, `seasonal_arc`, `inspection`, `stock_shortage` cards exhibited the same single-snippet collapse for their effect previews; and Mira's body + Mushroom Blight's body each collapsed to 2 lines because the optional `manner_note` slot's snippets all required two simultaneous voice extremes. Root cause: every Movement II `effectPreview` pool authored only 2-condition `effectKind|effectTag + voiceAxis` snippets with no kind-only base-rung fallback, so when one snippet matched it covered every effect of that kind on the card; `mannerNote` pools demanded two simultaneous voice extremes so neutral actors fired nothing. **New 8th structural gate `checkPreviewVariety`** at `src/cards/compose/gates/previewVariety.ts` (wired into `runAllGates` as an optional config block alongside the seven existing gates; exported through `src/cards/compose/gates/index.ts` alongside `PREVIEW_VARIETY_REASONS` / `PREVIEW_VARIETY_DEFAULTS`) — pure function over `(sampler, config) → GateReport` that simulates a multi-choice card render through `pickSnippet` with the synthetic per-effect slot ids and fires on two rules: `within_card_preview_collapse` (≥3 identical preview lines in a row within one card render) and `card_render_low_diversity` (unique-to-total ratio of rendered preview lines below the threshold). Defaults `minUniqueRatio: 0.15` and `maxIdenticalRun: 2` chosen to discriminate the screenshot defect (~0.04 ratio) from healthy multi-snippet pools (~0.25 with 6 snippets across 24 lines) without false positives on many-choice cards. **Pool repair across all 20 Movement II templates.** Every `effectPreview.ts` got 4-8 unconditional kind-only base-rung snippets covering `state_change` / `pressure` / `future_hook` / `cause` as applicable — the Phase-C FNV tie-break on per-effect synthetic slot ids `effect_preview::${slotId}::${idx}` now spreads across multiple candidates so different effects on the same card resolve to different snippets. Every `mannerNote.ts` got 3-4 `atLeast: 1` mid-rung snippets at explicit `specificity: 0` so neutral-voiced actors fire SOMETHING without outranking the extreme-rung snippets that prior tests depend on. The two-axis combo snippets (`manner_cold_sleeves`, etc.) preserved at specificity 2 so existing tests asserting specific snippets fire for specific profiles still pass. **Live regression test** at `tests/cards/compose/gates/previewVariety.live.test.ts` exercises five screenshot-defect templates (`staffAside`, `staffBurnout`, `areaAtmosphere`, `seasonalArc`, `inspection`, `stockShortage`) with realistic multi-choice multi-effect renders that mirror the screenshot card shapes. Five fixture tests at `tests/cards/compose/gates/previewVariety.test.ts` prove the gate fires on the two failure modes and stays silent on a healthy pool. **Out of scope** (documented in `docs/plans/voiced-surface-arc.md §Phase 18` and the user's `AskUserQuestion` scope decision): title verbosity (`MIRA THE RESOLUTE: LINGERS WITH SOMETHING TO SAY`), report prose, day beats, choice labels (already varied), the fallback card, the legacy composer (retired Phase 16). Phase 18 itself stays open as the standing playtest-driven phase; future iterations append additional repair entries to ISSUE-113. Tracked as ISSUE-113.

**Phase 143 (Voiced Surface arc, Phase 17, Cross-Situation Voice Consistency): done.** Movement IV centerpiece. New gate `checkCrossSituationVoice` at `src/cards/compose/gates/crossSituation.ts` plus harness `tests/cards/compose/gates/crossSituationHarness.ts` proving that "the same character speaks recognizably the same way across every situation they appear in." The harness registers the 11 migrated actor-voiced templates by actor kind — staff (4: staffAside, staffBurnout, foodSafetyCrisis, rumourCrisis), regular (3: drinkOrder, regularComplaint, rumourCrisis), customer_group (3: customerComplaint, violence, rumourCrisis), faction (3: factionRequest, inspection, rumourCrisis), supplier (2: supplierReliability, rumourCrisis), notable_npc (2: inspection, rumourCrisis) — and samples each archetype against the real pools. Five archetype voice profiles fixed by the gate: `terse_cold` (terseness 2, warmth 0, formality 1, floridity 0), `florid_warm` (terseness 0, warmth 2, formality 0, floridity 2), `formal_prickly` (terseness 1, warmth 0, formality 2, floridity 1), `neutral_tic_qualifies` (axes all 1, `verbalTic: qualifies_everything`), `neutral_tic_trails_off` (axes all 1, `verbalTic: trails_off`). **Three structural rules.** Rule 1 — voice-pole expression (LIVENESS): for each non-neutral archetype, at least one fired snippet must carry a `voiceAxis` condition matching one of the archetype's poles in `≥ ⌈templatesAvailable × 0.5⌉` of the actor's migrated templates. The check is per-archetype-overall (not per-axis-pole) so real pools authoring only one direction of an axis (the marked pole) don't trip false positives — the rule catches the meaningful case where a fixed voiceProfile is invisible across most situations. Rule 2 — tic surfacing (LIVENESS): a snippet conditioned on the archetype's tic must fire in ≥2 templates for ≥3-template kinds and ≥1 for 2-template kinds. Rule 3 — voice-blind pool detection (SAFETY): for each actor-voiced `(template, slot)` pair, compare rendered text from archetype `terse_cold` vs `florid_warm` across N samples (same seeds). If ≥80% identical, the pool is voice-blind; this is the "florid line reachable by terse-max actor" detector from the spec, scoped to slots whose pool contains at least one voice-axis or verbal-tic gate (narrator-only slots are excluded by design). **One small refactor** to `src/cards/compose/assemble.ts`: `pickSnippet` now delegates to a new internal `pickSnippetTrace` that returns the resolved `Snippet` (id + conditions + text). Public API unchanged — `pickSnippet` is a thin wrapper around `pickSnippetTrace(...)?.text`; all 153 pre-existing compose tests stayed green through the refactor. The gate uses `pickSnippetTrace` to read each fired snippet's `conditions` array and check whether any `voiceAxis` or `verbalTic` gate matches the archetype's pole-set, without re-running selection. **Sibling gate, not a `runAllGates` entry.** `runAllGates` is template-scoped (one report per template); cross-situation is multi-template by definition. The gate is exported from `src/cards/compose/gates/index.ts` alongside the seven existing gates but called directly from `tests/cards/compose/gates/crossSituation.test.ts`. **Three failure-fixture tests** prove each violation reason bites: `voice_axis_under_expressed` fires when no template gates on any of the archetype's poles; `tic_under_surfaced` fires when no template gates on the archetype's tic; `voice_blind_pool` fires when a pool with at least one voice gate has opposite-pole archetypes drawing identical text in ≥80% of samples. Four live-suite tests cover the real pools — gate passes, observed coverage records hit-count per archetype × kind, voice-blind pair fractions reported for every actor-voiced slot, and a determinism test asserts two calls produce identical violation lists. Full suite green at **2459/2459 across 199 files** (+7 vs the post-Phase-142 baseline of 2452: 7 new cross-situation tests). Tracked as ISSUE-112.

**Phase 142 (Voiced Surface arc, Phase 16, Ambient Surface & Legacy Retirement): done.** Closes Movement III by porting the last two surfaces off the Phase-95 voice composer and deleting the slice entirely. Three new single-slot narrator-voiced sections — `day.morning_empty`, `day.service_empty`, `day.closing_empty` at `src/reports/compose/sections/{morningEmpty,serviceEmpty,closingEmpty}.ts` with pools at `src/reports/compose/pools/<section>/line.ts` — replace the three `composeEmpty('morning'|'service'|'closing', voiceKey)` calls in `web/src/lib/screens/DayScreen.svelte:158-160`. Each pool ships the five legacy `EMPTY_STATE_POOLS[kind]` entries verbatim as unconditional fallback snippets (user-visible voice preserved), plus one or two `hasTag end_of_week` / `end_of_month` gated variants for milestone days. `DayScreen.svelte` now imports the three `compose<Beat>EmptyLine` helpers from the new top-level `src/reports/index.ts` re-export and keys each call on `closedDayOrdinal` + `isEndOfWeek` / `isEndOfMonth` derived from `state.calendar`, so the same calendar day reads the same line through all three beats on a refresh. The **fallback card** rewritten in place at `src/cards/templates/fallback.ts` as a compositional template — one severity-gated narrator-voiced `title` pool at `src/cards/compose/pools/fallback/title.ts` (unconditional + low / mid / high rungs via `severityBelow` / `severityAtLeast`); body lines pass through `seed.textIngredients.{sensoryDetails,recentContext,pressureContext ?? stakesReadable}` verbatim because the fallback by definition catches families that can't have per-family snippet pools, and the cards-contract truth rule holds by sourcing body text only from sim-emitted state. The retired Phase-95 `pickSeverityAdjective` title prefix is gone. **Legacy slice deleted:** the whole `src/cards/voice/` directory (`composer.ts`, `tonePools.ts`, `index.ts`), the eight-symbol re-export block at `src/cards/index.ts:68-80`, and `tests/cards/voice/composer.test.ts` are removed. New **retirement-guard test** at `tests/cards/voice/noLegacyImports.test.ts` walks `src/`, `web/src/`, and `tests/` recursively and fails if any source / test file imports from the retired path; the needle is split (`'/cards' + '/voice/'`) so the scanner doesn't match itself. **Out of scope** (documented in the phase plan and the ISSUE-111 tracker entry): the tavern log (`src/reports/tavernLogProjection.ts` + `web/src/lib/components/TavernLog.svelte`) is a pure projection over `state.history` whose row summaries are sim-emitted at write time — same carve-out shape as Phase 15's weekly/monthly notes; first-encounter hints (`web/src/lib/components/FirstEncounterHint.svelte`) display static glossary `oneLine` / `longer` definitions from `src/reports/glossary.ts` and tutorial text is deliberately stable for player learning. After Phase 16 nothing in the codebase imports `src/cards/voice/`. Full suite green at 2452/2452 across 198 files (+7 vs the post-Phase-141 baseline of 2445: 16 new day-beat tests + 10 new fallback tests + 1 new guard test − 20 deleted composer tests). Tracked as ISSUE-111.

**Phase 141 (Voiced Surface arc, Phase 15, Reports Prose): done.** Connector-only voicing across the projection-built daily-report surface. Seven new sections, each a single-slot snippet pool wired into a `ReportSection` via the existing `reportSectionAsTemplate` adapter (Phase 14's adapter, reused unchanged): **daily header** (the morning `opening` line), **daily quiet** (the full one-liner for low-activity days), **missed-opportunities pressure_remedy readable + secondary** (connector verb gated by action category + trend verb gated by delta magnitude), **missed-opportunities diff_counterfactual readable** (single-verb gated by action category, drops the `would have` frame the wrapper supplies), **missed-opportunities ignored_seed readable** (closing verb gated by severity band low/mid/high), **yesterday-digest secondary + coin verb** (reputation/pressure direction verbs + coin direction verb), **daily service-log Traffic / Service / Driver lines** (traffic verb gated by volume band, service verb gated by direction × magnitude band, driver phrase gated by direction). Every figure (coin, deltas, patron counts, severities, group counts, unpaid tab counts) stays sim-derived structural — the leading verb / phrase / connector is the only voiced piece. Eight pool directories at `src/reports/compose/pools/{missedOpportunity,yesterdayDigest,serviceLog}/`; section composers at `src/reports/compose/sections/{missedOpportunity,yesterdayDigest,serviceLog}.ts`. **Three legacy hand-rolled prose helpers deleted** (`composePressureReadable`, `composeDiffReadable`, `composeSeedReadable` plus their `actionLabelGuess` / `capitalize` helpers) — no remaining call sites. **Dedupe gate forced unique snippet text within each slot** (initial connector-pool draft had multiple `"would have eased"` variants distinguished only by gating condition; the dedupe gate runs structurally without considering conditions, so reworded to make every snippet's text canonically distinct). **Sim-emitted notes excluded from scope** — weekly signals notes (`src/sim/modules/weekly/signals.ts`), weekly community notes (`src/sim/modules/weekly/community.ts`), and monthly landlord/inspection/rival narrative (`src/sim/modules/monthly/`) live in sim modules, not the projection layer; voicing them would require sim-layer refactor that conflicts with Phase 15's "do not alter what reports measure" rule. FNV pathology surfaced during diversity testing: paired-axis variation in the periodKey (`d${i}.target_${i}`) returns the same mod-2 bucket for every i; fixed by using realistic stable target ids (`kitchen` / `cellar`) in tests, which is what production sees. Full suite green at 2445/2445 across 192 files (+131 vs the post-Phase-140 baseline of 2314). Tracked as ISSUE-110.

**Phase 140 (Voiced Surface arc, Phase 14, final Movement II migration): done.** Periodic & Narrative Beats cluster. Two compositional templates close Movement II: `monthlyReviewCard` is rewritten in place at `src/cards/templates/monthlyReview.ts` (id `monthly_review.monthly_review` — was `monthly_review.strategic`, realigned to the Phase-7+ `${family}.${type}` convention; voice register `office_quarters` shared with debtRent for the same end_month cadence) — replaces the legacy hand-written template that lifted `ti.calendarContext[0]` plus top-3 pressures formatted as raw `${label} ${value} (${trend})` strings into the body through `composeBody`. `seasonalArcCard` (new, `src/cards/templates/seasonalArc.ts`, id `seasonal_arc.arc_milestone`, voice register `civic_floor`) is the first dedicated card for the `seasonal_arc` family — pre-Phase 14, every seasonal_arc seed (both `arc_milestone` and `festival_preparation` types, both active-arc Path A and anticipation Path B at `expandedSeedGenerators.ts:4285-4489`, across all five themes mushroom_blight / miner_payday_boom / inspection_campaign / rival_tavern_expansion / festival_approaching) routed to `fallbackCard`. **One template covers both types and both paths**: `appliesTo.seedTypes` lists both `['arc_milestone', 'festival_preparation']`; the seed's primaryActor is a `local_event` ref (Path A) or `undefined` (Path B anticipation), neither carrying castAttributes — so the template is narrator-voiced and variety comes from theme tags + arc pressures + memories. Both templates carry the standard Phase-7+ four-slot body (title + sim-backed establishing_line + flavor reaction_line + optional flavor manner_note) plus Phase-6 composed choices/previews. Title prefixes: `Month in review: ${snippet}` (fixed for monthly_review, preserves the legacy header frame); `${arcLabel ?? themeLabel ?? 'Seasonal event'}: ${snippet}` for seasonalArc (mirrors `rivalTavern.ts:109-122`'s pattern — Path A reads `state.world.localEvents[ref.id]?.label`, Path B falls back to a title-cased theme label derived from `seed.toneHints[2]`). **Movement I loopback — none**: both seeds already expose what's needed through existing primitives. monthly_review reads every relevant pressure via `pressureRising` per ID (`landlord` / `debt` / `reputation_drift` / `staff_burnout` / `customer_complaint` / `rival_tavern_pressure`); seasonal_arc reads the five themes via `hasTag` against the theme already present in `seed.toneHints[2]` (`collectSeedTags` in `src/cards/compose/conditions.ts:80-88` reads `domain ∪ toneHints ∪ stake tags` — same precedent the Phase 13 record names for not needing new SignalIds), reaches arc_escalation / festival_readiness via `pressureRising`, distinguishes climax via `seedType arc_milestone`. **Sim-coherence co-conditions**: the gate's `STATE_LOOKUP_KINDS` excludes `hasTag` and `seedType`, so every sim_backed snippet gated only on a theme tag or climax type is paired with `pressureRising arc_escalation` (or `festival_readiness` for the festival theme), exactly the cultureConflict / reputationShift precedent. Twelve new pool files at `src/cards/compose/pools/{monthlyReview,seasonalArc}/` and two new specs at `specs/cards/{monthly_review,seasonal_arc}.spec.yaml`. 34 new template tests (16 monthlyReview + 18 seasonalArc), 4 new `runAllGates` blocks (two template-level + two ad-hoc choice-pool), four new sampler families in `tests/cards/compose/gates/samplers.ts` (state-perturbation tables for both narrator-voiced templates — 19 monthly entries × 15 seasonal entries), two new Phase-6 context builders per template, `Template 8 — monthlyReviewCard` block in `tests/cards/templates.test.ts` rewritten for compositional output and `Template 9 — seasonalArcCard` block added, `monthlyReviewCard voice` block in `tests/cards/templates.voice.test.ts` rewritten and `seasonalArcCard voice` added. Three snippets needed reworking during authoring: one sim-coherence pairing (`est_climax` gained `pressureRising arc_escalation` co-condition because `seedType` alone is not a state-lookup); two voice-bounds trims (seasonalArc `mnr_payday` 11→9 words, `mnr_arc_memory` 12→9 words); one unbacked-history-claim fix (monthlyReview `mnr_high_severity` "last month by half" → "today by a wide measure" — the `\blast\s+(week|night|month|time)\b` detector flagged the snippet because it had no memoryPresent/repeatCount condition); two choice-label trims (monthlyReview `label_pay_rent_due` 7→6; seasonalArc `label_raise_price_severity` and `label_ignore_default` 7→6). **Weekly overview deferred to Phase 15 (Reports Prose)** per user scope direction — voicing the WeeklyOverview ReportSection requires architectural work (snippet composition applied to a non-card report surface) that belongs to Phase 15's scope; Phase 14 stays a tight card-cluster migration. Movement II closes at 19 compositional templates in `REQUIRED_CARDS`. Tracked as ISSUE-109.

**Phase 139 (Voiced Surface arc, Phase 13, seventh Movement II migration): done.** Reputation, Rumour & Rivals cluster. The legacy `reputationShiftWeeklyCard` (which declared `timings: ['end_week']` while `generateReputationShift` at `issueSeedGenerators.ts:3510-3514` emits `timing: 'closing'` — so the card *never matched* in production and seeds routed to `fallbackCard`, the same alignment bug Phases 7 and 12 fixed) is deleted and replaced by a fresh compositional template at `src/cards/templates/reputationShift.ts` (id `reputation_shift.reputation_shift`, voice register `tavern_floor`, narrator-voiced because the seed has no `primaryActor` — reputation is tavern-wide per audit pass 1 §5.3). Two new dedicated templates land for families that previously routed to `fallbackCard`: `rumourCrisisCard` (new, `src/cards/templates/rumourCrisis.ts`, id `rumour_crisis.rumour`, voice register `tavern_floor`, actor-voiced through six possible castAttribute-bearing target kinds — supplier / regular / faction / staff / customer_group / notable_npc; custom predicate gracefully falls through to `fallbackCard` when target is `tavern_identity` / `rumour` / `system`, matching Phase-12's inspection/`systemRef` precedent; nine-slot response roster ties Phase-12's inspection for the largest verb surface in the codebase) and `rivalTavernCard` (new, `src/cards/templates/rivalTavern.ts`, id `rival_tavern.social_conflict`, voice register `office_quarters` mirroring `debtRentCard`'s closing-time strategic register, narrator-voiced because the seed's primaryActor is `local_event` for an arc-themed rival or `system` for the unnamed competitor — neither carries `castAttributes`). All three carry the standard Phase-7+ four-slot body (title + sim-backed establishing_line + flavor reaction_line + optional flavor manner_note) plus Phase-6 composed choices/previews. Title prefixes vary: `Reputation: ${snippet}` (fixed for reputation_shift), `${actorDisplay}: ${snippet}` (the rumour target's display name), and `${arcLabel|'Rival tavern'}: ${snippet}` (the rival arc's label or a literal fallback). **Movement I loopback (tag enrichment, no new band signals)**: three additive `seed.domain` entries — `reputation.${axisId}` on reputation_shift (one of `cozy` / `tasty` / `reputable` / `reliable` / `dangerous` / `respectable` / `scholarly` / etc.), `rumour.${dramatic.accuracy}` + `rumour.target.${target.kind}` on rumour_crisis (both paths — main + `rumourSeedFromRumour`), and `rival.arc` (when a rival local arc is active) or `rival.system` on rival_tavern — chosen over banding seven reputation axes / four accuracy enums / boolean activation because all three facts are enum/categorical, not numeric meters that band naturally; `hasTag` reads them directly without new SignalIds. No new condition primitives, no new `resolveActorRef` role strings, no new signal IDs. **Sim-coherence co-conditions**: the sim-coherence gate's `STATE_LOOKUP_KINDS` deliberately excludes `hasTag` (the rule is "every non-fallback sim-backed snippet has ≥1 state-lookup primitive" — `hasTag` is treated as seed-shape, not state), so every tag-only condition on a sim_backed slot was paired with `pressureRising` on the family's primary pressure (`reputation_drift` / `rumour_pressure` / `rival_tavern_pressure`), exactly the cultureConflict precedent at `pools/cultureConflict/establishingLine.ts:99-113`. Twenty-one new pool files at `src/cards/compose/pools/{reputationShift,rumourCrisis,rivalTavern}/` and three new specs at `specs/cards/{reputation_shift,rumour_crisis,rival_tavern}.spec.yaml`. 49 new template tests (15 reputationShift + 19 rumourCrisis + 15 rivalTavern), 6 new `runAllGates` blocks (three template-level + three ad-hoc choice-pool), `Template 7` block in `tests/cards/templates.test.ts` split into 7a (reputationShift) + 7b (rumourCrisis) + 7c (rivalTavern), `reputationShiftWeeklyCard voice` block in `tests/cards/templates.voice.test.ts` split into three new blocks, six new sampler families in `tests/cards/compose/gates/samplers.ts` (state-perturbation tables for reputation and rival; actor-perturbation against a supplier kind as representative for rumour mirroring the cluster's actor-voicing surface), 1 new `phase139.domainTags.test.ts` asserting the additive `seed.domain` tags appear on `generateReputationShift`'s output. Snippet rewording during authoring: four sim-coherence pairings (each axis snippet plus the two rival activation snippets — pair with `pressureRising`), two voice-bounds trims (manner_note `mnr_pressure_rising` 11→10 words, `mnr_embraced_memory` 11→10), two voice-bounds trims on rumour reaction line (`rxn_tic_quotes` 13→12, `rxn_tic_qualifies` 13→12). Full suite green at 2314/2314 across 179 files (+72 vs the post-Phase-12 baseline). Tracked as ISSUE-108.

**Phase 138 (Voiced Surface arc, Phase 12, sixth Movement II migration): done.** Crises & Safety cluster. The legacy `foodSafetyCrisisCard` (which composed the body from `ti.sensoryDetails[0]` + `ti.recentContext[0]` + `ti.stakesReadable[0]` through the Phase-95 `composeBody` and prepended a `pickSeverityAdjective` to the title — the high-severity fragment-dump-with-adjective-glue pattern) is rewritten in place at `src/cards/templates/foodSafetyCrisis.ts` as an actor-voiced compositional template (id `food_safety.crisis`, voice register `back_of_house`, custom predicate requires cook with `castAttributes`). Two new dedicated templates fill the families that were routing to `fallbackCard` pre-Phase 12: `violenceCard` (new, `src/cards/templates/violence.ts`, id `violence.customer_incident`, voice register `tavern_floor`, actor-voiced via customer-group cohort `castAttributes` from Phase 128) and `inspectionCard` (new, `src/cards/templates/inspection.ts`, id `inspection.inspection_threat`, voice register `civic_floor`, actor-voiced via faction or notable_npc `castAttributes`; predicate rejects `systemRef('inspector')` so the system-fallback seed cascades to `fallbackCard`). All three carry the standard Phase-7+ four-slot body (title + sim-backed establishing_line + flavor reaction_line + optional flavor manner_note) plus Phase-6 composed choices/previews. Body shape: `${actorDisplay}: ${snippet}` title, prefix-and-extend per Phase 5. **Severity-gated gradient**: food_safety adds a second severity ceiling at `severityAtLeast: 85` paired with `pressureRising food_safety` for the "unsafe to serve from" rung — the highest gradient yet in the arc. **Movement I loopback:** one new band signal added — `customer_group.rowdiness` — wired through `src/sim/signals/{types,bands,numeric,query,index}.ts` mirroring the Phase-8 customer_group bands; default-thirds thresholds `[40, 70]`. The violence picker scores groups by `patronage + rowdiness` (`issueSeedGenerators.ts:2128`) so without the band the violence establishing line could only anchor on pressure or satisfaction proxies. **Alignment fix**: legacy `foodSafetyCrisis.appliesTo.timings: ['during_service']` mismatched the generator's `morning_prep` emit; Phase 12 aligns the template to what the generator actually emits, the same trick Phase 7 applied to staffBurnout's `closing`-vs-`morning_prep` mismatch. Twenty-one new pool files at `src/cards/compose/pools/{foodSafety,violence,inspection}/` and three new specs at `specs/cards/{food_safety,violence,inspection}.spec.yaml`. 38 new template tests (13 foodSafety + 13 violence + 12 inspection), 6 new `runAllGates` blocks (three template-level + three ad-hoc choice-pool), six new sampler families in `tests/cards/compose/gates/samplers.ts` (actor-perturbation for cook staff / customer_group / faction following the Phase 7 / 8 / 10 patterns), 3 new signal-surface tests for the `customer_group.rowdiness` band. Four snippets needed trimming during authoring to clear the voice-bounds gate (em-dashes count as words; two reaction-line snippets started at 13 words). Two establishing-line snippets needed sim-coherence co-conditions (`hasTag inspection_relevant`, `severityAtLeast 85`) — neither is a state-lookup primitive on its own, so each gained a `pressureRising food_safety` co-condition. Full suite green at 2242/2242 across 175 files (+38 vs the post-Phase-11 baseline). Tracked as ISSUE-107.

**Phase 137 (Voiced Surface arc, Phase 11, fifth Movement II migration): done.** Premises & Atmosphere cluster. The legacy `maintenanceWarningCard` (which claimed both the `maintenance` and `area_atmosphere` families through one shared `appliesTo`, glued `ti.sensoryDetails[0]` + `ti.pressureContext[0]` + `ti.stakesReadable[0]` through the legacy `composeBody`, and prepended a deterministic `pickAreaStateAdjective` to the title for the raw-fragment-dump-with-adjective-glue pattern the arc was built to retire) splits into two compositional templates partitioned by family: `maintenanceCard` (new, `src/cards/templates/maintenance.ts`, id `maintenance.maintenance_problem`, voice register `back_of_house`, narrator-voiced because areas have no `castAttributes`) and `areaAtmosphereCard` (new, `src/cards/templates/areaAtmosphere.ts`, id `area_atmosphere.warning`, voice register `back_of_house`, also narrator-voiced — first dedicated card for the family; the legacy template was written against `maintenance_problem`'s text-ingredient shape and mishandled atmosphere's `cleanliness X` recentContext + `namedEntities` block). Body shape is the standard Phase-7+ four-slot pattern (title + sim-backed establishing_line + flavor reaction_line + optional flavor manner_note) plus Phase-6 composed choices/previews. Titles render as `${area.label}: ${snippet}` — the area label anchors the room being voiced, mirroring Phase 9/10's actor-label prefix. **Movement I loopback:** one new band signal added — `area.damage` — wired through `src/sim/signals/{types,bands,numeric,query,index}.ts` mirroring the existing `area.condition` / `area.cleanliness` bands; default-thirds thresholds `[40, 70]`. The maintenance seed picks worst area by `damage + (60 - condition)`; without the new band the establishing line could only anchor on condition. `area.smell` / `area.risk` were not added — smell folds into reaction-line flavor; risk is reachable via `severityAtLeast`. **Role-resolution extension** (also Phase-1 surface, additive): `resolveActorRef` in `src/cards/compose/conditions.ts` now recognises the `'location'` role string as a fallback to `seed.location` — required because the maintenance/area_atmosphere seeds carry no `primaryActor` and no `namedEntities` entry, but their `seed.location: areaRef(...)` is the actor the narrator-voiced card centres on. Without this extension `signalEquals area.damage|area.cleanliness role: 'location'` could not resolve. Twelve new pool files at `src/cards/compose/pools/{maintenance,areaAtmosphere}/` and two new specs at `specs/cards/{maintenance,area_atmosphere}.spec.yaml`. 36 new template tests (18 maintenance + 18 areaAtmosphere), 4 new `runAllGates` blocks (two template-level + two ad-hoc choice-pool), `Template 4 — maintenanceWarningCard` block in `tests/cards/templates.test.ts` replaced by 4a (maintenanceCard) + 4b (areaAtmosphereCard), `maintenanceWarningCard voice` in `tests/cards/templates.voice.test.ts` replaced by two new voice blocks, four new sampler families in `tests/cards/compose/gates/samplers.ts` (state-perturbation tables × 14 entries each covering area meters × pressures × memories × tone tags × severity), 3 new signal-surface tests for the `area.damage` band. Three snippets needed rewording during authoring to clear the voice-bounds gate (`rxn_fallback`, `rxn_pressure_rising`, `mnr_neglected_memory`, `mnr_cleaning_memory` all started over-budget by 1-3 words); one needed rewording to clear the sim-coherence `\byesterday\b` unbacked-temporal detector (`mnr_pressure_rising` "thicker than yesterday" → "sits thick today"). Full suite green at 2203/2203 across 172 files (+67 vs the post-Phase-10 baseline). Tracked as ISSUE-106.

**Phase 136 (Voiced Surface arc, Phase 10, fourth Movement II migration): done.** Factions & Culture cluster. The legacy `factionRequestCard` (which lifted raw `textIngredients` through `composeBody` and prefixed the title with a `pickFactionRelationNoun` lookup, and whose name resolver guarded on `kind === 'faction'` so `culture_conflict` seeds — primaryActor `culture` — fell through to a bare `ti.subject` title) splits into two compositional templates: `factionRequestCard` (rewritten in place at `src/cards/templates/factionRequest.ts`, id `faction_request.social_conflict`, voice register `tavern_floor`, actor-voiced via faction `castAttributes` from Phase 128, `custom` predicate requires the resolved faction carry voice — graceful fallback otherwise) and `cultureConflictCard` (new, `src/cards/templates/cultureConflict.ts`, id `culture_conflict.social_conflict`, voice register `civic_floor`, narrator-voiced because cultures have meters but no `castAttributes` — they're population concepts, not individuals). Both tighten `appliesTo` to `timings: ['during_service']` and `seedTypes: ['social_conflict']`, dropping the legacy `relationship_test` admission that no real generator emits. Body shape is the standard Phase-7+ four-slot pattern (title + sim-backed establishing_line + flavor reaction_line + optional flavor manner_note) plus Phase-6 composed choices/previews. **Movement I loopback:** three new band signals added — `culture.tension`, `culture.comfort`, `culture.familiarity` — wired through `src/sim/signals/{types,bands,numeric,query,index}.ts` mirroring the existing customer_group / regular bands; default-thirds thresholds `[40, 70]`. Without them the cultureConflict establishing_line could only anchor on `pressureRising cultural_tension` and `memoryPresent`; with them it states the culture's actual standing in the room. No new faction bands — `faction.relationship` / `faction.influence` (Phase 127) plus memory tags (gratitude / grudge / refusal) drove every rung. Fourteen new pool files at `src/cards/compose/pools/{factionRequest,cultureConflict}/` and two new specs at `specs/cards/{faction_request,culture_conflict}.spec.yaml`. 35 new template tests (17 factionRequest + 18 cultureConflict), 4 new `runAllGates` blocks (two template-level + two ad-hoc choice-pool), `Template 6 — factionRequestCard` block in `tests/cards/templates.test.ts` splits into 6a (factionRequest) + 6b (cultureConflict), `factionRequestCard voice` in `tests/cards/templates.voice.test.ts` splits the same way, six new sampler families in `tests/cards/compose/gates/samplers.ts` (actor-perturbation for faction mirroring Phase 7/8/9's supplier; state-perturbation for culture with a pre-computed 19-entry table covering culture meters × pressures × memories × calendar tags × severity), 9 new signal-surface tests for the three new culture bands. Two snippets needed rewording during authoring to clear the sim-coherence gate's `\bagain\b` unbacked-history detector (`mnr_tic_qualifies` "straighten it again" → "twice, then once more"; `title_cultural_rising` "surfacing again" → "in the room"); the two festival/ritual establishing-line snippets gained a `pressureRising cultural_tension` co-condition because `hasTag` alone is not a state-lookup. Tracked as ISSUE-105.

**Open work** — see [`docs/ISSUE_TRACKER.md`](docs/ISSUE_TRACKER.md) for full evidence, scope, dependencies, and test approach:

1. **Tier 4 Progressive Onboarding arc** — ISSUE-060…077, phases 99–116. Locked design contract at [`docs/plans/progressive-onboarding.md`](docs/plans/progressive-onboarding.md). Reframes Day 1 as "first time opening a tavern" and unlocks simulation systems one at a time across the first ~10 weeks of in-game time.
2. **Tier 6 Living Cast arc Phases F–G** — locked roadmap at [`docs/plans/living-cast-arc.md`](docs/plans/living-cast-arc.md), framework contract at [`docs/plans/card-composition-framework.md`](docs/plans/card-composition-framework.md). Phase A (ISSUE-090, phase 121), Phase C (ISSUE-092, phase 123), Phase D (ISSUE-093, phase 124), Phase E (ISSUE-094, phase 125), and Phase F's first situation — staff_aside (ISSUE-095, phase 126) — are done; Phase B is the hand-authored convergence artifact at `docs/plans/living-cast-arc-phase-b.md` (no Claude Code involvement); subsequent Phase F situations and Phase G land as separate ISSUE entries when they start.

The tracker carries **88 issues across 6 tiers** (Tier 0 infrastructure; Tier 1 + 1.5 roster grows + Rare Ingredients; Tier 2 family rewrites; Tier 3 polish + audit; Tier 4 onboarding; Tier 5 UI/UX clarity; Tier 6 Living Cast). 66 are `done`, 17 are `open`, 5 are `superseded` by the Rare Ingredients arc. ISSUE_TRACKER.md is the authoritative source for what changes; the "Current work" callout at the top of that file names the next-up issue.

**Phase numbering.** ISSUE-001…057 follow `phase = 40 + N` (phases 41–97). Phase 98 belongs to the retroactive ISSUE-080. Tier 4 onboarding (ISSUE-060…077) → phases 99–116. Tier 5 UI/UX clarity (ISSUE-078/079) → phases 117–118. ISSUE-058/059 take the next free integers, phases 119/120. Tier 6 Living Cast Phase A (ISSUE-090) → phase 121, Phase C (ISSUE-092) → phase 123, Phase D (ISSUE-093) → phase 124, Phase E (ISSUE-094) → phase 125, Phase F first situation (ISSUE-095) → phase 126 (phase 122 is reserved for the hand-authored Phase B artifact). **Phase numbers are a file-naming convention; execution order is set by ISSUE-NNN ordering and the `Depends on` chains, not by phase number** — start with whatever the tracker's "Current work" callout names, even if its phase number is higher than a later-tier issue.

**Locked design contracts** (read these before planning work in their scope):

- `docs/plans/phase-01-simulation-contract.md` — vision, pillars, core gameplay model (foundational, phase 1).
- `docs/plans/phase-21-expansion-contract.md` — expansion arc (phases 21–40): identity, culture, place, and relationships become persistent state, not card flavour.
- `docs/plans/rare-ingredients-economy.md` — Tier 1.5 arc (ISSUE-025…033, phases 65–73).
- `docs/plans/cards-contract.md` — bridge between headless sim and card UI layer (for the eventual card-layer work).
- `docs/plans/game-loop-and-ux.md` — game loop / UX working contract (§8–9 explicitly open).
- `docs/plans/progressive-onboarding.md` — Tier 4 arc (ISSUE-060…077, phases 99–116); amends `game-loop-and-ux.md §2.1`.
- `docs/plans/living-cast-arc.md` — Living Cast arc roadmap (Phase A = ISSUE-090 / phase 121, Phase C = ISSUE-092 / phase 123, Phase D = ISSUE-093 / phase 124, Phase E = ISSUE-094 / phase 125, Phase F first situation = ISSUE-095 / phase 126, all done; further Phase F situations + Phase G upcoming). Pairs with `card-composition-framework.md`.
- `docs/plans/card-composition-framework.md` — locked compositional framework for the card layer (slots, snippets, deterministic assembly). Sits below `cards-contract.md`; resolves its §9 "tone/presentation pipeline" question.

Per-issue workflow: the user puts Claude Code in plan mode, Claude reads the matching tracker entry, produces a phase plan file in `docs/plans/`, then implements. Update the issue's `Status` and `Phase` fields in the tracker inline as work progresses; closed issues stay in the tracker as history.

Work proceeds against whatever branch is checked out; coordinate branch and merge decisions with the user before pushing or opening pull requests.

## Core Design Rule

> The simulation is the source of truth. Cards reveal, interpret, escalate, or resolve simulation truth — cards must not invent truth.

A card may surface a problem the sim already understands. A card may **not** apply arbitrary effects, contradict known state, or pretend past decisions did not happen. Keep this rule in mind for every change.

Phase 21 extends this principle for the expansion arc: **identity, culture, place, and relationships must become persistent simulation facts before they become card flavour.** Generated people and world entities are state, not throwaway display strings.

## Architectural Rules

These apply to all simulation code from Phase 2 onward:

1. **Pure by default.** No DOM, React, browser storage, network, timers, global mutable state, or `Math.random()` inside simulation logic.
2. **Serializable state.** `TavernState` must be plain JSON-compatible data. No class instances, functions, Maps/Sets, or circular references in state.
3. **Modular systems.** No god-files. Each module registers its own state defaults, phase hooks, actions, report sections, and (later) issue seed generators.
4. **Registries for expandable concepts.** Areas, stock items, customer groups, staff roles, owner actions, reputation axes, pressure types, issue seed families, and sim modules all go through registries — never hardcode expandable lists into the engine.
5. **Deterministic RNG.** Use a seeded PRNG (`prando` preferred, `seedrandom` acceptable) threaded through `SimContext`. Same seed + same input = same result. Replay must work.
6. **Causality.** When a major value changes, the sim must record *why*. Cause entries feed future reports, tooltips, and card text.
7. **Named RNG streams for identity** (Phase 24+). Identity-style randomness — names, supplier rosters, regular customer rosters — must use named streams via `ctx.getRngStream(streamId)` so an extra service roll doesn't shift a generated name. `ctx.rng` remains for ad-hoc per-day rolls.
8. **Persistent identity is state, not display** (Phase 21+). Generated people (staff, suppliers, regulars, NPCs) and named world entities (cultures, factions, local arcs) live in `TavernState`. Generate names once at creation, store them, reuse them — never regenerate when a report is re-viewed.

The engine shape future code targets:

```ts
const result = simulateDay(previousState, playerInput, runConfig)
// result: { state, reports, diffs, issueSeeds, debug }
```

## Repo Layout

```
docs/
  ISSUE_TRACKER.md                                                      # Authoritative repair-pass tracker — 79 issues, current work
  P20F1.md                                                              # Investigation: response_impact gate at 28/70 (2026-05-13)
  plans/
    phase-01-simulation-contract.md                                     # LOCKED: vision, pillars, core gameplay model (phase 1)
    phases-02-05.md                                                     # Project structure, calendar, RNG, state
    phases-06-10.md                                                     # Areas, stock, economy, customers
    phases-11-15.md                                                     # Staff, service loop, owner actions, weekly, monthly
    phases-16-20.md                                                     # Memories, causes, pressures, issue seeds, card-ready output
    phase-21-expansion-contract.md                                      # LOCKED: expansion arc vision and scope rules
    phases-22-25-expansion-structure-calendar-rng-state.md              # Content folders, calendar tags, RNG streams, world state
    phases-26-30-expansion-validation-hooks-areas-suppliers-cultures.md # Cross-ref validation, new phase hooks, area traits, suppliers, cultures
    phases-31-35-expansion-staff-scenes-projects-community-arcs.md      # Staff identity, service scenes, owner projects, weekly community, seasonal arcs
    phases-36-40-expansion-memory-attribution-pressures-seeds-readiness.md # Entity-scoped memory, attribution, pressure webs, expanded seeds, final readiness
    rare-ingredients-economy.md                                         # LOCKED: Tier 1.5 design contract (ISSUE-025…033, phases 65–73)
    cards-contract.md                                                   # LOCKED: card-layer contract (8 templates; for eventual card work)
    game-loop-and-ux.md                                                 # Working contract: game loop / UX; §8–9 open
    progressive-onboarding.md                                           # LOCKED: Tier 4 design contract (ISSUE-060…077, phases 99–116)
    seven-pass-investigation-plan.md                                    # Cross-cutting audit that informed ISSUE-034…057
    phase-53-59-tier2-followups.md                                      # Perf notes from Tier 2 pass (phase20 OOM, phase40 runtime) — not tracked as ISSUE
    phase-41-*.md … phase-120-*.md                                      # Per-phase plans, mostly one per ISSUE-NNN in ISSUE_TRACKER.md (created as each phase starts)

src/sim/                # Phases 1–40 (initial build + expansion) + repair-pass extensions; headless, pure
  core/                 # engine, context, rng, phases, diff, effect, reports, types
  state/                # TavernState, schemas (zod), validation, migrations
  registries/           # Registry<T> + concrete registries (areas, stock, customers, staff, actions, pressures, …)
  modules/              # Domain modules: areas, attribution, calendar, causes, cultures, customers,
                        # economy, factions, feedback, history, issues, issueSeeds, localArcs,
                        # memories, monthly, ownerActions, pressures, regulars, reports, responses,
                        # service, staff, stock, suppliers, weekly, world
  content/              # Identity / world-content (Phase 22+): naming, cultures, factions, suppliers,
                        # npc, staff, tavern, events, text
  testing/              # simRunner, createTestState, runDay/Week/Month, fixtures
  utils/                # ids, math, clamp
tests/sim/              # phase2.structure.test.ts … phase97.*.test.ts (per-phase coverage through the repair pass)
web/                    # Svelte web UI (phases 87–98, 117–118; Tier 4 onboarding will land here)
```

## Working on This Repo

- **Read the relevant phase doc before implementing.** Each phase doc has explicit "Acceptance Criteria" and "Do Not Do" sections. Respect both.
- **Repair-pass work (phases 41–120, ongoing) is driven by `docs/ISSUE_TRACKER.md`.** Before planning a repair phase, read the matching `ISSUE-NNN` entry — it carries the full Evidence, Impact, Scope, Depends on, and Test approach. The tracker's `Depends on` field is hard: a dependency issue must reach `done` before dependent work starts. Update an issue's `Status` and `Phase` fields inline as work progresses; closed issues stay in the tracker as history, not noise. Phases 65–73 additionally implement against the locked rules in `docs/plans/rare-ingredients-economy.md`, and phases 99–116 against `docs/plans/progressive-onboarding.md` — per-issue phase plans for those arcs reference the design doc rather than restating it.
- **Do not skip ahead.** Don't add cards, UI, narrative text, or issue-seed *content* before the phase that introduces them. The plans are sequential for a reason.
- **No `Math.random()` in sim code.** Even for one-off helpers — use the seeded RNG from context.
- **No browser/runtime dependencies in `src/sim/`.** The simulation must run headless in tests and (eventually) in any host environment.
- **Keep state serializable.** If you find yourself reaching for a `Map` or class, convert at the boundary.
- **Use named RNG streams for identity** once Phase 24 lands. Do not call `ctx.rng` for name generation or anything whose stability across re-renders matters — use `ctx.getRngStream(streamId)`.
- **No card prose in Phases 21–40 either.** Phase 21 explicitly forbids finished card text across the whole expansion arc; produce structured "text ingredients" only until the card layer is built.
- **Integrate additively during the expansion arc.** Phases 28–35 call for light, additive integration with existing modules (e.g. "do not deeply rewrite customerModule"). Add fields and hooks rather than rewriting working systems.
