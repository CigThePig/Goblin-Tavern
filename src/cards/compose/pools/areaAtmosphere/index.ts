// Phase 137 / ISSUE-106 — Voiced Surface arc, Phase 11 (Premises & Atmosphere).
//
// Slot manifest for the area_atmosphere compositional template. First
// dedicated card for the family — pre-Phase 11 every area_atmosphere
// seed was nominally claimed by the legacy `maintenanceWarning` template
// but rendered through its `maintenance_problem`-shaped body code, which
// mishandled atmosphere's `cleanliness X` recentContext readout.
// Narrator-voiced (areas have no `castAttributes`); design record at
// `specs/cards/area_atmosphere.spec.yaml`.

export { titlePool as areaAtmosphereTitlePool } from './title'
export { establishingLinePool as areaAtmosphereEstablishingLinePool } from './establishingLine'
export { reactionLinePool as areaAtmosphereReactionLinePool } from './reactionLine'
export { mannerNotePool as areaAtmosphereMannerNotePool } from './mannerNote'
export { choiceLabelPool as areaAtmosphereChoiceLabelPool } from './choiceLabel'
export { effectPreviewPool as areaAtmosphereEffectPreviewPool } from './effectPreview'
