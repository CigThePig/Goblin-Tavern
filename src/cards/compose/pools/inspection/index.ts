// Phase 138 / ISSUE-107 — Voiced Surface arc, Phase 12 (Crises & Safety).
//
// Slot manifest for the inspection compositional template. First
// dedicated card for the `inspection.inspection_threat` seed family —
// pre-Phase 12 these seeds routed to `fallbackCard`. Actor-voiced via
// the inspecting faction's or notable NPC's `castAttributes`
// (Phase 128); design record at `specs/cards/inspection.spec.yaml`.

export { titlePool as inspectionTitlePool } from './title'
export { establishingLinePool as inspectionEstablishingLinePool } from './establishingLine'
export { reactionLinePool as inspectionReactionLinePool } from './reactionLine'
export { mannerNotePool as inspectionMannerNotePool } from './mannerNote'
export { choiceLabelPool as inspectionChoiceLabelPool } from './choiceLabel'
export { effectPreviewPool as inspectionEffectPreviewPool } from './effectPreview'
