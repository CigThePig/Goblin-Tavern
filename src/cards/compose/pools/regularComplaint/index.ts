// Phase 134 / ISSUE-103 — Voiced Surface arc, Phase 8 (Regulars & Complaints).
//
// Slot manifest for the regular_complaint compositional template.
// Replaces (with `customerComplaint/`) the legacy customerComplaint
// hand-written template. The matching spec at
// `specs/cards/regular_complaint.spec.yaml` records the design.

export { titlePool as regularComplaintTitlePool } from './title'
export { establishingLinePool as regularComplaintEstablishingLinePool } from './establishingLine'
export { reactionLinePool as regularComplaintReactionLinePool } from './reactionLine'
export { mannerNotePool as regularComplaintMannerNotePool } from './mannerNote'
export { choiceLabelPool as regularComplaintChoiceLabelPool } from './choiceLabel'
export { effectPreviewPool as regularComplaintEffectPreviewPool } from './effectPreview'
