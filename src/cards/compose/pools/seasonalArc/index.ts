// Phase 140 / ISSUE-109 — Voiced Surface arc, Phase 14 (Periodic & Narrative Beats).
//
// Slot manifest for the seasonal_arc compositional template. First
// dedicated card for the family — pre-Phase 14 every seasonal_arc seed
// (both `arc_milestone` and `festival_preparation` types, both
// active-arc Path A and anticipation Path B) routed to `fallbackCard`.
// Narrator-voiced — local_event refs (Path A primaryActor) and the
// undefined-primaryActor anticipation path neither carry castAttributes.
// Design record at `specs/cards/seasonal_arc.spec.yaml`.

export { titlePool as seasonalArcTitlePool } from './title'
export { establishingLinePool as seasonalArcEstablishingLinePool } from './establishingLine'
export { reactionLinePool as seasonalArcReactionLinePool } from './reactionLine'
export { mannerNotePool as seasonalArcMannerNotePool } from './mannerNote'
export { choiceLabelPool as seasonalArcChoiceLabelPool } from './choiceLabel'
export { effectPreviewPool as seasonalArcEffectPreviewPool } from './effectPreview'
