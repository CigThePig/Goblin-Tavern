// Phase 135 / ISSUE-104 — Voiced Surface arc, Phase 9 (Suppliers, Stock & Debt).
//
// Slot manifest for the stock_shortage compositional template. First
// dedicated card for the family — pre-Phase 9 every stock_shortage seed
// routed to the fallback. Narrator-voiced (no primaryActor on the seed,
// so voice axes never fire). Design record at
// `specs/cards/stock_shortage.spec.yaml`.

export { titlePool as stockShortageTitlePool } from './title'
export { establishingLinePool as stockShortageEstablishingLinePool } from './establishingLine'
export { reactionLinePool as stockShortageReactionLinePool } from './reactionLine'
export { mannerNotePool as stockShortageMannerNotePool } from './mannerNote'
export { choiceLabelPool as stockShortageChoiceLabelPool } from './choiceLabel'
export { effectPreviewPool as stockShortageEffectPreviewPool } from './effectPreview'
