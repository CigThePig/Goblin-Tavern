// Phase 138 / ISSUE-107 — Voiced Surface arc, Phase 12 (Crises & Safety).
//
// Slot manifest for the violence compositional template. First
// dedicated card for the `violence.customer_incident` seed family —
// pre-Phase 12 these seeds routed to `fallbackCard`. Actor-voiced via
// the customer group's `castAttributes` (Phase 128); design record at
// `specs/cards/violence.spec.yaml`.

export { titlePool as violenceTitlePool } from './title'
export { establishingLinePool as violenceEstablishingLinePool } from './establishingLine'
export { reactionLinePool as violenceReactionLinePool } from './reactionLine'
export { mannerNotePool as violenceMannerNotePool } from './mannerNote'
export { choiceLabelPool as violenceChoiceLabelPool } from './choiceLabel'
export { effectPreviewPool as violenceEffectPreviewPool } from './effectPreview'
