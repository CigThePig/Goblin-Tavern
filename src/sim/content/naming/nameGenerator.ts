// Phase 22 §"Naming Types" — name generator placeholder.
//
// Phase 22 only reserves the seam. Phase 24 §"Name Generator Starter"
// defines `generateName(profile, rng, generatedBy)` against the seeded
// `names`/`*_identity` RNG streams. Keeping the signature out of Phase
// 22 avoids importing `SimRng` before the streams API exists.

export {}
