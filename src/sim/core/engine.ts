// Phase 2 placeholder. The real engine is wired up in later phases; for now
// this file exists so future imports of `runSimulation` have a stable home.

import type { SimulationResult } from './result'

export function runSimulation(): SimulationResult {
  throw new Error('runSimulation is not implemented yet — see docs/plans for the relevant phase.')
}
