import { describe, it } from 'vitest'

// TODO(Phase 5): fill in TavernState default factory / cloning / serialization tests.
describe.skip('Phase 5 — Base Tavern State', () => {
  it('placeholder for Phase 5 state tests', () => {
    // intentionally empty
  })
})
